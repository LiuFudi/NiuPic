# 安全策略

## 支持范围

NiuPic 只对**最新发布版本**提供安全修复。旧版本请先升级：

| 版本 | 安全更新 |
| --- | --- |
| 最新版（见 [README](README.md) 徽章 / 发布页） | ✅ |
| 更早的版本 | ❌ 请先升级到最新版 |

## 怎么报告漏洞

**请不要开公开 issue。** 用下面任一私密渠道：

- GitHub 私密漏洞报告：<https://github.com/LiuFudi/NiuPic/security/advisories/new>
- 或发 issue 只写"我有一个安全问题想私聊"，不要写细节，我看到后会给联系方式

请在报告里尽量给出：

1. 版本号（应用内「关于」或 `niupic/manifest` 里的 `version`）
2. 复现步骤（越具体越好，含请求样例时请打码 token 与真实路径）
3. 影响判断：能读到什么、能改到什么、是否需要已登录
4. 如果有 PoC，附上；没有也行

**响应时间**：我尽量 7 天内回第一条消息，确认后 30 天内给出修复版本或缓解方案。
这是个人维护的项目，不承诺 SLA，但不会看了不吭声。

## 这个应用的攻击面（先看这里，能省一轮）

NiuPic 是**跑在你自己 NAS 上、只服务你自己的应用**：界面通过飞牛统一网关打开（桌面图标，复用飞牛登录态），服务本身只监听**本机回环地址**（`127.0.0.1`）与一个仅供网关使用的 Unix socket（权限 600），**不对局域网开端口**。
它的设计前提是"使用者就是 NAS 的主人"。以下**不算漏洞**，除非你能证明越权：

- 已登录用户能浏览/删除自己素材库里的文件（这是功能）
- 应用需要读你授权的目录（fnOS 的应用权限机制，安装后要手动授权）
- 局域网内其他设备能打开登录页（同网段直连，与应用中心其它应用一致）
- 已知的 `X-Powered-By: Express` 之类的指纹信息

**算漏洞的**（欢迎报告）：

- 未登录即可读写接口（鉴权绕过、token 伪造、越权访问别的库）
- 目录穿越：通过接口读到**未授权目录**下的文件（`../`、软链接、绝对路径）
- SQL 注入、命令注入（尤其扫描/缩略图/文件名相关路径）
- 上传功能写入可执行文件、或在授权目录之外落盘
- 口令/密钥泄露：配置文件、日志、错误响应、随包分发的文件里出现明文口令或私钥
- 依赖中的已知高危漏洞**且在本项目里真的可达**

## 部署建议

- 应用不对局域网开端口，正常情况下无需关心端口暴露；如果为了调试临时用
  `HOST=0.0.0.0` 启动过，**记得改回来**（那不是给日常使用准备的）。
- 远程访问请用 fnOS 自带的远程访问；应用自己的访问口令请设强一些 ——
  网关负责「用户已登录飞牛」，应用口令是第二道门，两道都要。
- 首次安装后**立刻**在应用里设置访问口令；不要长期使用默认口令。
- 定期备份素材库里的 `.niupic/` 目录（索引与缩略图），它丢了要重扫才能重建。
- 应用以 fnOS 应用账号（非 root）运行，不要为了"省事"手工改成 root 运行。

## 我不做的

- 不接收"帮我打补丁"的匿名压缩包；补丁请走 PR 并说明意图。
- 不提供付费安全支持。

---

最后更新：2026-09-22
