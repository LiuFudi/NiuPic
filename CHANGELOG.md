# 更新日志

本项目由 [LiuFudi](https://github.com/LiuFudi) 接手维护，
上游原始项目为 [ZangXincz/FlyPic](https://github.com/ZangXincz/FlyPic)（已归档）。

---

## 2.6.0

### 改名：对外「牛图库」、界面内「NiuPic」

同一个软件在两个场合被看到，需要的名字其实不一样：在应用中心与桌面上要短、要好认；
在应用界面里保持项目本名。从这一版起统一成两套名字，并把口径写下来（免得下次又各写各的）：

| 场合 | 名字 |
| --- | --- |
| 桌面图标、应用列表、应用中心介绍，以及界面里"点桌面上的 X 图标"这类指引 | **牛图库** |
| 应用界面内部（顶栏、登录页、关于页、错误提示、打赏文案） | **NiuPic** |
| 仓库与项目文档（README / NOTICE / SECURITY / CONTRIBUTING / design / FUNDING / package.json / 迁移脚本） | **NiuPic** |
| README 顶部标题 | **NiuPic · 牛图库** |
| 官网（对外介绍这个软件的页面） | **牛图库** |
| CHANGELOG 里**已发布的历史条目** | **不动**（规范 1.3：只增不改） |

- `manifest` 的 `display_name` 由 `NiuPic` 改为 `牛图库`，`app/ui/config` 的桌面入口 `title` 同步；
  应用介绍（`desc`）开头也改成「牛图库 - …」。
- **`appname` 一个字没动**（仍是 `niupic`）：它是应用 ID、目录名与网关路径名，改了在飞牛眼里
  就变成一个全新应用（用户的素材库列表、授权目录都得重配）。桌面入口的键名也必须保持
  `niupic.Application` —— 规范要求入口名以 `appname` 开头。
- README 里原来的「牛图 NiuPic」全部改为「NiuPic」，顶部标题按规则保留为「NiuPic · 牛图库」。

### 新增：打赏弹窗里的反馈入口

- 「打赏完全自愿，不影响任何功能，也不会改变软件的任何行为。」这句**一字未改**，
  后面**另起一行**：`如果遇到BUG，欢迎前往Github反馈或者添加QQ群：818299505`。
- 其中 **Github 是可点链接**（指向本项目仓库），**Github 与 QQ 群号都显示为红色** ——
  用的是项目自己的 `--err` 红（与顶栏那个 ❤ 打赏按钮同色），不额外引入颜色。
- 文案仍集中在一个文件里（`frontend/src/config/donate.json`），但**拆成段落**而不是塞一段 HTML：
  界面侧用 JSX 渲染，不需要 `dangerouslySetInnerHTML`，也就没有多出注入面。
- README 的「打赏支持」一节同步补了这一行。**红色只在应用界面里生效** ——
  GitHub 会剥掉 Markdown 里的 `style` 属性，仓库页上做不到单独染色；该节仍控制在 5 行以内（规范 4.4）。

### 验证

- 前端单测新增 4 项断言：反馈整句与要求逐字一致、Github 链接地址正确、
  **红色类确实被组件渲染出来**（只写在配置里不算数）、原来那句未被改动。
- 后端与前端的既有用例全绿（后端 95 通过 / 0 失败 / 18 跳过；前端 145 通过）。

## 2.5.5

### 修复：四处"改了 A 忘了 B"

这一版没有新功能，全是收尾。前几次重构（2.4.2 路径安全、2.4.4 接网关、2.5.0 格式清单收敛、
2.5.1 换回收站）各自留下了一处"另一半没跟着改"，而且四个症状都很容易被误判成别的问题。

**① 拖拽上传在网关下必然失败（基路径漏了第五处）**

2.4.4 把"图片 URL / 复制图片 / socket.io / 接口请求"四处路径收敛到 `appBase()` 时，
漏掉了上传：它走的是另一个 axios 实例、URL 写死 `/api/upload`。应用挂在 `/app/niupic`
之后，这个请求打到的是**平台根路径**，应用根本收不到 —— 后端日志里连一条记录都没有，
而用户看到的是"拖进去没反应"。

**② 重命名可以写到素材库之外（写路径没有护栏）**

`path.join(directory, newName)` 会老老实实处理 `..`，而 `newName` 直接来自请求体：
传 `../../x.jpg` 能把文件搬出素材库，目标同名时还会覆盖库外文件。2.4.2 给读路径装了
`resolveInside`（`..`、软链逃逸都拦住了），**写路径漏了同一道闸**。
现在新名称只允许是"名字"：含分隔符、`..`、空值一律 403，最终路径再走一次 `resolveInside` 兜底。

**③ 文件监控会把库里的记录删掉（扩展名白名单与 formats.js 不一致）**

监控器自己维护了一份约 40 个后缀的白名单（注释还写着"与 scanner.js 保持一致"），
而入库的口径是"所有文件都收"。它算删除的方式是"库里的路径 − 白名单扫出来的路径"——
于是该目录下所有不在白名单里的文件（相机 RAW、tga/exr/qoi 那批、以及各类"其他"文件）
都被当成已删除、**从数据库里清掉**；用户看到的是照片莫名消失、重启又回来。
现在清单从 `src/config/formats.js` 取（唯一真源），并且在判定删除前**落盘确认一次**：
白名单只说明"我们认不认得这个类型"，不能拿它当"文件还在不在"的判据。

**④ 原图/预览 URL 没做编码（文件名里有 `#` `?` `%` 就打不开）**

`照片#1.jpg` 的 `#` 之后被浏览器当成锚点丢掉；`a?b.jpg` 的 `?` 之后变成查询串；
`50%.jpg` 会让 Express 解参数时抛 URIError。缩略图不受影响（文件名是 md5），
所以现象是"网格里有图、双击却打不开"，完全看不出是编码问题。现在逐段 `encodeURIComponent`。

### 修复：其余几处

- **`/api/file/restore` 参数不合法时返回 500 而不是 400**：`ValidationError` 忘了导入，
  抛的是 ReferenceError。用户看到"服务器内部错误"，排查方向被带偏。
- **`config.json` 迁到 `etc/`（`TRIM_PKGETC`）**：飞牛卸载时不删 `etc/`，配置放这里天然
  扛得住"升级会先跑一遍卸载流程"。老位置 `var/` 保留为兜底（etc 不可写时自动退回），
  并且首次启动会把 `var/` 那份**复制**过来（是复制不是移动，老文件留着）。
  卸载向导选"全部删除"时两处一起清，护栏不变（只认 `…/@appconf/<应用名>` 与 `…/@appdata/<应用名>`）。
- **`cmd/main` 按规范 4.3 补齐**：`status` 校验进程身份（`/proc/<pid>/cmdline`）**并且**
  要求 `app.sock` 存在；`start` 用 `setsid` 脱离父进程、等 socket 就绪（10 秒超时）、
  失败如实返回 1；`stop` 扫 PID 文件之外的游离进程；node 运行时按"版本目录 → 扫
  `/var/apps/nodejs_v*` → PATH"三级查找（坑 4）；PID 改由子进程自己写
  （`setsid` 必要时会 fork，用 `$!` 会记到错的进程）。
- **素材库根目录的文件变动现在也能被发现**：轮询只跟踪子目录，往库根拷文件要等重启或手动同步。
- **`server.js` 的 `module.exports` 被覆盖**：`isShuttingDown` 导出看着有、实际是 undefined。
- **`app/ui/config` 的 `control.accessPerm` 由 `editable` 改为 `readonly`**：应用是
  `allUsers=false` 的管理员应用，官方建议就是 `readonly`；而且 `editable` 隐含
  "用户可以改设置、应用要跟着生效"，对应的是 `config_callback` —— 本项目的
  `config_init` / `config_callback` 是空壳（没有平台侧配置项），声明 editable 名不副实。
- **惯例清理不再每 5 秒强制 5 次 GC**：改为 60 秒一次，且只有显式打开 `devMode` 才强制 GC
  （发行包不带 `--expose-gc`，所以线上原本只是空转；开发时才是真吃 CPU）。

### 构建

- `build-fpk.js` 构建期强制 `cmd/* = 755`、其余文件 644（规范 9.3 与坑 15：
  fnpack 按源文件权限打包，在 Windows/WSL 下解开源码包再构建会丢掉执行位，本项目犯过两次）。
- 版本号加入"纯三段 `x.y.z`"校验（上架要求第 6 条），构建期就拦住 `x.y.z-beta` 这类后缀。

### 文档与站点（都会让新用户按错误的方式装/配）

- **网站 `website/index.html` 与现状对齐**：页脚仍写 `MIT`（2.3.10 起已是 GPL-3.0-or-later）、
  仍在说"安装时可自定义应用端口（默认 15002）"（2.4.8 已收掉端口，只走统一网关）、
  仍在教"添加素材库时需输入飞牛文件系统的完整路径"（2.4.x 起是从**已授权文件夹列表**里选），
  页眉页脚的仓库链接也还是旧地址（问题反馈会打到不存在的仓库）。
- **`CONTRIBUTING.md` 的日志路径写错了**：写的是 `@appdata/niupic/logs/`，
  但应用并不写那个目录 —— 日志是 `cmd/main` 写的 `info.log`（另有平台侧的
  `/var/log/apps/niupic.log`）。让贡献者去一个不存在的目录找日志，等于把排查卡在第一步。
- **`package-lock.json` 的版本号与许可证停在 2.3.9 / MIT**：锁文件里那份"根包元数据"
  自 2.3.10 起就没再跟着 `package.json` 走（规范 1.2：版本号只能有一处真源，
  其它地方引用它）。现在同步成 2.5.5 / GPL-3.0-or-later。
- **README 补一条说明**：2.5.5 起配置在 `etc/`，历史那条 `@appdata` 的备份命令
  要按 `@appconf` 用，否则用户备份的是已经不再更新的旧文件。

### 安全

- **去掉交付物里的真实内网 IP**：2.5.2 写"会话失效"排查页时，把作者自己 NAS 的
  `IP:端口`当成了文案里的"示例"（本条目按规范 6.1 不再复述该地址），
  于是它**随界面发给了所有人**；同一个地址还留在 `gatewayRecovery.test.js` 的夹具里。
  排查提示不需要具体地址，改成"同一个 IP、同一个端口"就够了。
  本轮交付前的全局扫描（对发布树 grep 内网网段）把它扫了出来 —— 这类东西肉眼不会去看。
- **历史条目里的主机专属路径一并改成通用写法**：2.3.x 那几版把排查现场的真实路径
  （备份目录名 + 素材库名 + 机型 + 原始文件名）原样写进了变更日志，而这些会随源码包
  发到所有人手里。规范 6.1 对交付物的要求是"绝对不能出现"，规范 1.3 的"只增不改"
  则是不让历史失真 —— 两者撞上时这里只做**遮蔽**、不动技术事实：
  路径换成 `/vol1/1000/Photos/IMG_0001.JPG` 这类通用示例，机型与真实文件名去掉，
  其余一字未改（排查结论、复现步骤、实测数字都保持原样）。

### 构建与环境（补齐构建环境后真打了一次包，四个坑全是 Windows 才暴露的）

- **`fs.cpSync` 会把 Node 进程直接打崩（Node 22.x + Windows）**：打包脚本第 3 步复制
  `backend/src` 时以 `0xC0000409`（STATUS_STACK_BUFFER_OVERRUN）fail-fast 退出 ——
  抛不出任何 JS 异常、`try/catch` 拦不住，脚本打印两行就无声结束（退出码 -1073740791）。
  同一目录改用 `fs.promises.cp`（或手写递归）正常，现在统一走异步版。
  实测环境：node v22.23.3 / Windows 10 19045（"跨平台"这句承诺必须真的验过才算数）。
- **Windows 版 fnpack 会把 `cmd/main` 打成 0666（没有执行位）**：应用中心是直接执行
  `cmd/main` 的，这样的包装上去**根本起不来**（规范 9.3 与坑 15 记的就是这个现象）。
  Linux 版 fnpack 会保留源文件权限位，所以在 Linux 上打包完全看不出来。
  现在构建期做一次**权限位修正 + 自校验**：只改 tar 头的 mode 字段并重算头部 checksum，
  不碰任何文件内容，因此 manifest 的 `checksum`（= md5(app.tgz)）依旧有效；
  修完再回读确认 `cmd/main` 是 755，不是就让构建失败。
- **跨平台解析 Linux 依赖需要 pnpm ≥ 10**：`supportedArchitectures` 在 pnpm 9 上不生效，
  sharp 的 `@img/sharp-linux-x64` 不会安装，构建最后停在
  「缺少 @img/sharp-linux-x64，依赖未按 linux x64 解析」—— 这个报错完全不提 pnpm，
  实测时很容易往依赖解析/镜像方向查很久。现在构建脚本先检查 pnpm 主版本，太旧就直接
  把原因与升级命令写出来。
- **根 `package-lock.json` 里有一条编造记录**：锁文件写着 `content-type@1.2.0` 和一个
  `resolved` 指向 `content-type-1.2.0.tgz`，而该版本**在 npm 上不存在**
  （已发布的是 …1.0.5、2.0.0…；`body-parser@1.20.3` 真实要求是 `~1.0.5`，
  锁里那条 integrity 恰好是 1.0.5 的哈希）。后果是任何 `npm ci` / `npm install`
  都会 404 失败 —— 本项目一直用 pnpm 构建才没被踩到。已按 registry 实际数据修正。
- **`npm test` 不退出（看起来像卡死）**：`database/db.js` 的 WAL checkpoint 定时器、
  `database/dbPool.js` 的空闲连接清理定时器、`AuthService` 的登录记录清理定时器都没
  `unref()`。后台维护任务不该成为"进程必须活着"的理由 —— 之前只要测试里 new 了
  `LibraryDatabase` 又没走到 `close()`，进程就永不退出（实测挂在 `scan-stats` 那个文件上）。
  三者都 unref 后，`npm test` 3 秒内自行结束。生产环境不受影响（HTTP 服务撑着事件循环）。

### 验证（本机实测，2026-09-25）

环境：Windows 10 19045 · Node v22.23.3（npm 10.9.9）· pnpm 10.34.5 · fnpack 1.2.3（Windows 版）。

| 项目 | 结果 |
| --- | --- |
| 前端单测（vitest） | **141 / 141 通过**（15 个文件，含新增的网关恢复用例与"未定义符号"静态检查） |
| 前端构建（vite 5.4.21） | 通过（`dist/`：602 kB JS + 48 kB CSS） |
| 后端单测（`node --test`） | 113 项：**95 通过 / 0 失败 / 18 跳过**，4.8 秒自行结束（`npm test` 不再"卡死"） |
| 打包（`node scripts/build-fpk.js`） | 通过，产出 `dist/niupic_2.5.5_x86.fpk`（14.71 MB） |
| fpk 结构自查 | `manifest.version = 2.5.5`、`checksum` == `md5(app.tgz)` ✓、`cmd/* = 755` ✓、其余 644 ✓、目录 755 ✓、**无软链接** ✓、无 pnpm 记账文件 ✓、顶层 19 个条目 |

**那 18 条跳过全部是"Windows 上不具备这个能力"，不是产品缺陷**（在 Linux/fnOS 上会真正执行，
它们是安全与扫描回归的一部分）。为了让本机的 `npm test` 能信，顺带把测试本身的可移植性补齐了：

| 跳过项 | 根因 | 处理 |
| --- | --- | --- |
| `accessible-folders` 8 条 | mock 网关监听 **Unix socket**：Windows 上 Node 的 AF_UNIX 走命名管道，`listen(C:\…\x.sock)` 直接 EACCES | **改成真的能跑**：Windows 上用 `\\.\pipe\…`（前 3 条协议契约用例现在在 Windows 上也执行）；另 5 条依赖"POSIX 绝对路径"夹具（产品只认 `/vol…` 这类路径），明确跳过并写清原因 |
| `safe-path` 1 条（安全回归） | 建符号链接在 Windows 需要管理员/开发者模式（`EPERM`） | 能力探测 + 只跳过这一条（原来会让**整个文件**加载失败） |
| `scan-stats` 2 条 | `chmod` 在 Windows 上不改变写权限，造不出"写不出缩略图"的场景；Windows 会锁住 `metadata.db`，清理时 `EBUSY` | 权限场景明确跳过；清理改成"先关数据库再删目录 + 失败不报错" |
| `scanner-walk` 2 条 | 同上（改权限造不可读目录）；以及断言写 `/` 而 Windows 返回 `\` | 权限场景明确跳过；断言统一按 `/` 归一（产品入库前本来就会归一，是**用例**的可移植性问题） |

⚠️ **仍待真机复验**（本机没有 fnOS，以下必须上 NAS 做，结论以实测为准）：

1. 应用中心安装 `niupic_2.5.5_x86.fpk`，确认**能启动**（权限位修正是否真的解决了坑 15）；
2. 模拟网关（保留 `/app/niupic` 前缀 + 支持 WebSocket 升级）下用真浏览器拖一张图上传；
3. 重命名传 `../../x.jpg`，期望 403 且库外没有新文件；
4. 库内放一张 `.cr3`，再触碰该目录 mtime，断言记录仍在；
5. 从 2.5.4 升级安装一次，确认配置从 `var/` 搬到了 `etc/` 且素材库列表、访问口令都还在；
6. 故意改坏 `manifest.version` / `CHANGELOG` 各跑一次构建，确认版本校验真的以非零码退出。

## 2.5.4

### 改进：把"会话失效"的恢复做成一键（用户实测：原来要手动删 Cookie）

2.5.3 上线后用户的反馈很直接：

> 把 cookie 删除后，完整的重新登陆才解决。这个有点麻烦，普通用户还不一定会操作。

也就是说：说明虽然能看懂，但**恢复动作本身**要求用户会"清 Cookie + 完整重登" ——
这不是普通用户该做的事。这一版把能自动的自动化、把必须人工的点到一下：

**① 能自动好的，先自动试一次**

桌面打开应用时地址里带着一次性凭据（`/app/niupic?token=…`）。会话失效时，
那次请求本来会把会话重新种上 —— 所以遇到网关拒绝时**先自动重载一次**：

- 只试一次（`sessionStorage` 打标记），失败就老老实实显示说明页，**不会转圈**；
- 地址里没有凭据时不重载（重载也没用，直接引导用户）。

**② 必须人工时，一步到位**

- 主按钮改为直达平台**登录页** `http://<NAS>:5666/login`，而不是桌面首页 ——
  会话卡住时桌面首页可能仍然显示"已登录"的老界面，用户会以为"我明明登录着"；
- 新增**「清除本机登录信息后重新登录」**：清掉 localStorage 里的 token 与本页能清的 Cookie，
  再打开登录页。这正是用户手动做的那套动作，现在一键完成。
  （HttpOnly 的平台会话 Cookie 页面清不掉 —— 那是浏览器规矩，所以才需要重新登录让它换一份新的。）

**③ 文案按"照着做"重排**，并保留可折叠的诊断信息。

### 验证

- 前端 132 → **141 项**：新增 `src/utils/gatewayRecovery.test.js` 9 项
  （凭据识别、只自动重载一次、隐私模式下不跳、清理动作覆盖各 Path、登录页地址）；
- `e2e-gateway.cjs` 41 → **46 项**：断言链接直达 `/login`、有一键清理按钮、
  并新增一节"地址里带凭据时自动重载**不转圈**"（页面加载次数 ≤ 2、「只试一次」标记已写入、最终显示说明页）。

---

## 2.5.3

### 改进：把"网关会话失效"那一页做成能照着做的

2.5.2 已经能识别这种情况并显示说明，但真机上用户看到之后仍然卡住 ——
因为那一页只有**一段纯文本地址**（要自己抄到地址栏）、也没提"桌面页面本身可能是缓存的"。
这一版把它补成一份"照着做"的清单：

- 桌面地址改成**可点链接**，外加一个「打开飞牛桌面」按钮（新标签页打开）；
- 三条排查步骤，按真机上最可能踩到的顺序排：
  ① 桌面看起来是旧的 / 登录后毫无反应 → 在桌面按 `Ctrl+Shift+R` 强制刷新再登录；
  ② 确认桌面与应用是**同一个地址**（同 IP 同端口）——换过地址登录，凭据不通用；
  ③ 换一个**无痕窗口**重新登录，排除浏览器禁用 Cookie / 拦截脚本；
- 说明"别直接刷新这一页"（这页的凭据已经失效，刷新只会再失败一次），
  以及"从桌面图标打开"才是拿新凭据的方式；
- 新增**可折叠的诊断信息**：请求路径、HTTP 状态、网关原始响应片段。
  真机上它显示的就是 `请求：/app/niupic/api/auth/status`、`HTTP：200`、`响应：invalid token` ——
  一眼就能看出"不是应用挂了，是网关把请求拦了"。

### 验证

`e2e-gateway.cjs` 37 → **41 项**：新增断言覆盖"桌面地址是可点链接""提示强刷""提示无痕窗口"
"说清一次性凭据""带诊断信息"，仍然保持"界面上没有 `is not valid JSON`""不误导成后端没启动"
"界面稳定不闪"三条底线。

---

## 2.5.2

### 修复：平台网关会话失效时，界面给的是一句天书 + 一直闪的 logo

用户报的现象：用密码登录后"连接服务异常"，再次打开显示
`Unexpected token 'i', "invalid token" is not valid JSON`，并且登录页 logo 一直闪。

### 排查：先看平台到底回了什么，而不是猜前端

在 NAS 上直接问网关（不带任何会话）：

```
$ curl -i http://127.0.0.1:5666/app/niupic/api/health
HTTP/1.1 200 OK
Content-Type: text/plain; charset=utf-8
Content-Length: 13

invalid token
```

**根因在平台侧**：飞牛网关（nginx → `trim_http_cgi`）在所有 `/app/niupic*` 请求上校验平台会话，
会话无效时回的是 **HTTP 200 + 纯文本 `invalid token`**（不是 401、不是 JSON）。
触发点是 **NAS 在 16:56 重启**（`uptime` 与 accountsrv/nginx 的启动时间都能对上），
浏览器里那份平台会话失效了 —— 而前端 SPA 是缓存的，页面照常渲染，于是每一个接口调用都撞上这 13 个字节。

应用自己有三个 bug 把这件"会话过期"放大成了"看不懂的崩溃"：

**1. 客户端假设响应一定是 JSON**

```js
const response = await response.json();   // ← 撞上 "invalid token" 直接抛
if (!response.ok) { if (response.status === 401) { /* 登出 */ } }
```

`json()` 先抛，**后面判断状态码的代码一行都执行不到**：401 该触发的登出、该给的提示全跳过；
更危险的是 200 + 非 JSON 会被当成"成功但数据为空"。

现在统一"先当文本读，再尝试解析 JSON"，并按内容分类：

| 响应 | 处理 |
| --- | --- |
| 200 + `invalid token` | 报 `GATEWAY_SESSION_INVALID`：「飞牛网关会话已失效，请回到飞牛桌面重新登录后再打开牛图」 |
| 任意状态 + HTML | 报 `GATEWAY_HTML_RESPONSE`：多半是网关把请求拦下了，提示从桌面图标打开 |
| 其它非 JSON | 报 `NON_JSON_RESPONSE`，带上状态码与响应片段（不再抛 JS 引擎的报错） |
| 401（无论 JSON 与否） | 照旧清 token + 回登录页 |

**2. 登录页 logo 无限重试 → 一直闪**

```jsx
<img src="/ICON_256.PNG" onError={(e) => { e.target.src = '/favicon.png'; }} />
```

两个地址都是**绝对路径**，而应用挂在 `/app/niupic/` 之后，实测这两个地址在平台根路径上
都返回桌面的 HTML（404）→ 解不出图 → 触发 `onError` → 又把 src 设成同样 404 的地址 →
**无限循环，logo 无限闪**。

现在：地址走 `withBase()` 带上网关前缀；降级**只做一次**，两个都没成时画一个占位徽标
（不再有碎图，也不再循环）。顺带修掉「关于」页里同样写死的 `/LICENSE.txt`
（网关形态下那个链接点开是 404）。

**3. 网关拦下请求时，界面"装作没事"**

`AuthWrapper` 遇到非 401 的错误会**假设未设置口令并放行**，`App.jsx` 则盲目重试 5 次
（每次重画界面 → 就是用户看到的闪烁），最后给出的还是"请检查后端是否启动"——
方向完全错了：后端好得很，是会话失效。

现在识别到网关拦截就：停止重试、显示一页说明（回飞牛桌面重新登录 → 从桌面图标打开 →
不要用书签直连 + 「重试」按钮），并且**不再假装已登录**。

### 验证

- 前端 123 → **132 项**：新增 `src/api/client.test.js` 9 项，把"非 JSON 响应该怎么表现"钉死
  （含 401 + 非 JSON 也必须走登出逻辑）；
- `e2e-gateway.cjs` 22 → **37 项**，新增三节：
  - 登录页 logo 在**网关前缀**下真的加载出来（`/app/niupic/ICON_256.PNG`，complete=true）、
    没有碎图、2 秒后状态不变（**证明确实不闪了**）；
  - 「关于」页协议链接带前缀，且该地址经网关可取到全文；
  - 用网关模拟器把 `/api/` 全部改成 `invalid token`（复现真机场景）：
    断言出现说明页、文案给出可照做的下一步、**界面上没有 `is not valid JSON`**、
    没有把问题误导成"后端没启动"、界面稳定不闪。

### 给用户的排查经验（记在纪要里）

"应用打不开"先分清是**哪一层**在拒绝：应用日志里没有请求记录，就说明请求根本没到应用 ——
真机上正是这样（登录成功后只剩内存日志），于是去问网关，一问就问出了那 13 个字节。

---

## 2.5.1

### 修复：三处"看着没事、其实有事"的问题

这一版没有新功能，全是核对"全量重扫怎么 5 秒就完了"时顺出来的问题。

**1. 扫描汇总把"处理失败"算成了"跳过（没变过）"**

真机上核对数量时发现的：色图库磁盘上 **6279** 个文件，库里只有 **6269** 行 —— 少 10 个
（9 张 4K BMP + 1 张损坏 PNG）。日志里它们每次扫描都在报错，而汇总行写着：

```
全量重扫完成: 0 更新 / 6279 跳过（没变过） / 0 清理 / 0 失败      ← "0 失败"，但少了 10 个文件
❌ 处理图片失败: aerith82-pink4k.bmp (28ms) Input buffer contains unsupported image format
```

原因：`processImage` 失败时返回的是 `{status:'error'}`（不是抛异常），而重扫统计只区分
`processed` 和"其它一律 skipped"。于是**失败被算成"没变过"**，用户在界面上永远看不到
"N 个文件处理不了"。

现在三种结果分开：`processed`（入库）/ `skipped`（没变过，跳过是对的）/ `errors`（真失败），
失败时日志列出文件名与原因（最多 20 条，返回值里带前 20 条明细），界面提示也分开说：

```
扫描完成：找到 6279 个文件，跳过 6269 个未改动，10 个处理失败
（悬停可见具体是哪几个文件、为什么失败）
```

顺带把增量同步（`syncLibrary` / `quickSync`）也补上了 —— 那两处原来**直接把
`processImage` 的返回值丢掉**，新增文件解不开时同步照样报"成功"。

**2. 删除文件的"移入回收站"这一步在飞牛上一直是失败的**

`trash` 包按 freedesktop 规范要写 `~/.local/share/Trash`，而飞牛的应用账号**没有家目录**：

```
❌ 清理失败 __scan_probe__.jpg: EACCES: permission denied, mkdir '/home/niupic'
```

后果有两层，都是慢慢烂掉的：文件永远卡在素材库的 `.niupic/temp_backup` 里（删除没真正
生效），而且**每分钟重试一次、每次都记一条 ERROR** —— 真机上单个文件累计 **8052 条**，
另有 6 个文件各 1.1 万条以上，`info.log` 被刷到 53MB。

现在改成两种目的地，**都是同设备改名**（不复制、不需要家目录、不占额外空间）：

| 目的地 | 什么时候用 |
| --- | --- |
| 平台的回收站（从素材库往上找 `#recycle`） | 平台启用了回收站时。文件按「素材库名/原相对路径」摆好，用户能在文件管理里看见 |
| `<素材库>/.niupic/trash/<日期>/` | 平台没有回收站时（本机当前就是这样）。旁边写一份 `.trashinfo.json` 记着原始路径 |

挪不动时**只记一次**（原因写进 meta，之后不再重试），文件留在原地不丢；
`cleanExpiredTempFiles` 的返回值与日志能说清"移走几个、还剩几个没挪走"。
界面里"恢复失败"的提示也改成实话：文件在素材库的 `.niupic/trash/<日期>/`，
而不是"已移入系统回收站"（这台机器上并没有那个去处）。

顺带：`trash` 依赖（连它的 10 个传递依赖）不再需要，已从依赖里去掉 —— 成品少约 1MB，
要审的第三方包也少了。

**3. 解不开的图仍然入库，不再从库里消失**

这是 2.5.0 引入的、方向正确的行为变化，这一版把它写进用例钉住：文件解不开时不再
"整个文件不入库"，而是入库 + 占位图缩略图 —— 用户至少能看见"这个文件在这里、打不开"，
而不是它凭空消失。真机验证（拿色图库里那 3 个原先没入库的文件实测）：

```
49.png        → 5450x2160 image 缩略图=14772B     （损坏 PNG：真尺寸 + 占位图）
aerith1-4k.bmp → 2400x4000 image 缩略图=32586B     （2.5.0 起 BMP 走 ffmpeg）
aerith2-4k.bmp → 2400x4000 image 缩略图=33820B
扫描: 处理 3 / 跳过 0 / 失败 0 / 共 3（2.5s）
```

也就是说：**升级后对色图库做一次全量重扫，那 10 个文件就会全部进来**
（9 张 BMP 正常解码，那张损坏的 PNG 以占位图入库）。

### 验证

- 后端单元测试 95 → **111 项**（新增回收 7 项、扫描统计 5 项、临时文件夹清理 4 项）
- 前端 113 → **123 项**（把扫描结果提示抽成 `utils/scanHint.js` 并补 10 项单测）
- 真机实测：把卡住的 `__scan_probe__.jpg` 连同它的 meta 复制到临时库跑新代码 →
  `移入回收站 1 个；回收目录 …/.niupic/trash`，meta 清掉、原文件挪走、无需家目录；
  再用色图库那 3 个没入库的文件实测扫描 → 全部入库（见上）

---

## 2.5.0

### 新功能：格式支持对齐专业看图工具（参考 JarkViewer）

在此之前，"支持哪些格式"这件事是**三份清单各写一遍**：`constants.js` 的 `SUPPORTED_FORMATS`、
`thumbnail.js` 的 `IMAGE_FORMATS`、`rawPreview.js` 的 `RAW_EXTENSIONS`。同一件事写三遍，
改一处忘一处是必然的 —— 而且能出缩略图 ≠ 点开看得见：HEIC（iPhone 拍的默认格式）、
各家相机 RAW、PSD、TIFF 这些，双击进去是一整屏白。

这一版做了三件事：

**1. 格式清单收敛成唯一真源** `src/config/formats.js`

```js
getImageDecoder(ext)   // 'sharp' | 'ffmpeg' | 'raw' | 'unsupported' —— 谁来解码
getCategory(ext)       // image / video / audio / document / design / other
isBrowserRenderable(ext)
getFfmpegCodecHint(ext)
```

清单只写**真机上实测过能处理**的格式，判断依据是 sharp（libvips 8.15.3）与 ffmpeg 8.1.1
实际的解码器列表，不靠"看起来像图片"猜。顺带修掉两处因为清单不一致而产生的 bug：
"把文件夹复制进素材库"只收 7 种后缀（RAW/视频/PSD/HEIC 全都静默不入库）、
上传时也不给这些格式生成缩略图。

**2. 解码分三条路，三条都有兜底**

| 解码器 | 覆盖格式 |
|---|---|
| sharp / libvips | jpg png apng webp gif tif tiff svg heic heif hif avif avifs |
| ffmpeg | psd tga(含 targa/icb/vda/vst) exr hdr pic jp2(j2k/jpf/jpx/jpc) qoi dds dpx bmp dib pcx pnm/ppm/pgm/pbm/pam/pfm/pxm xbm xpm xwd wbmp sun/ras ico/icon |
| 内嵌预览 | 相机 RAW 39 种（cr2 cr3 nef arw dng orf raf rw2 r3d…） |

- 相机 RAW：抽文件里内嵌的 JPEG 预览（比占位图强得多，也是唯一可行解，没有 RAW 解码器）；
- PSD：先试 ffmpeg 解整张，不行再退回作者内嵌的预览图；
- TGA 家族：`image2` 解复用器靠扩展名认格式，`.targa/.icb/.vda/.vst` 会认不出来 ——
  失败后显式指定 `-c:v targa` 重试一次（实测这是唯一能救回这些后缀的办法）。

**3. 新增转码预览路由 `GET /api/image/preview/:libraryId/*`**

前端**不再自己判断"哪个格式能显示"**（`RightPanel` 里原来有一份 `['jpg','jpeg','png','webp','gif']`，
于是 HEIC/TIFF/AVIF/BMP/SVG 在右侧面板里永远只有缩略图）：

- 浏览器认的格式 → 后端原样发原图（不转码、保留动图、支持 Range）；
- 不认的 → 转 webp 下发，缓存在 `.niupic/previews/<散列前2位>/<md5>.webp`；
- 缓存"比源文件旧就重转"：同名文件被替换后自动自愈，不会一直显示旧图；
- 转不出来回 422、不是图片回 415 —— 前端据此退回占位图，而不是显示一张空白。

### 顺带修掉的问题

- **缩略图与预览共用一套解码逻辑**：`decodeStillWithFfmpeg` 只有一份，"显式指定解码器重试"
  这类细节不会出现"缩略图能出、点开却没有"的前后不一致；
- **ffmpeg 会把小图放大**：`scale=4096:4096:force_original_aspect_ratio=decrease` 对 400×200
  的图会输出 4096×2048（缓存里凭空多出几十倍体积）。改成 `min(4096,iw)` 后保持原尺寸；
- **中文/空格路径的预览**：新路由与 `/original` 一样过 `resolveInside` 路径校验，
  越界回 403（不是 500）；未鉴权一律 401；
- 全局错误处理兜底把 `PathEscapeError` 翻成 403 —— 以后哪个路由忘了 catch，也不会
  把"有人在试探目录穿越"记成"服务器内部错误"。

### 明确的边界（不支持的格式，不装样子）

`jxl`（JPEG XL）、`jxr`、`wp2`、`blp`、`cur`、`livp`：这套 sharp 与 ffmpeg 都没有对应解码器，
写进支持列表的后果是"点了打不开"，比不支持更糟。它们仍会入库（文件不丢），
走占位图 + 「在系统默认应用中打开」。

### 验证

- 后端单元测试新增 30 项（清单自检 11 + 真实解码 6 + 预览转码 13），共 94 项全绿；
- 新增端到端用例 `e2e-formats.cjs`（107 项）：从"造出 15 种格式的真实文件"开始，
  一路验到**缩略图像素、预览接口字节、查看器 canvas 回读像素**——
  解错文件、解成空白、退化成占位图都会被抓住；
- 前端 113 项、生命周期 19 项、全部 16 个端到端用例通过。

---

## 2.4.8

### 合规：收掉对外端口，改成纯网关形态

上架要求第 2 条是「优先接入统一网关，**且不要监听公网端口**」。2.4.4 已经接入网关，
但端口还留着（当时刻意留作过渡后备 —— 网关能不能转发只有真机点一次桌面图标才知道）。
现在网关在你的机器上验通了，于是把端口彻底收掉：

- `manifest` **去掉 `service_port = 15002`** —— 纯网关形态本来就不该声明端口
  （参考实现 fn-fancontrol 也没设）；
- `server.js` 的 TCP 监听改为**只绑回环地址**（`127.0.0.1`）：端口只留给本机调试与平台自检，
  局域网连不上；需要临时直连时显式 `HOST=0.0.0.0` 启动（注释里写明了不要长期这么用）；
- `wizard/install` **去掉「应用端口」字段**：桌面入口不再用端口，留着只会让人以为要记端口；
- `install_init` 删掉对 `ui/config` 里 `port` 的那行 `sed` —— 网关形态下 `ui/config` 没有
  `port` 字段，这行已经是死代码；
- `SECURITY.md` 的部署说明同步改成"回环 + 网关 socket"。

**唯一入口从此是桌面图标**（飞牛统一网关 → `app.sock`）。万一将来 fnOS 升级导致网关路由失效，
回退办法是重装一版带端口的旧包，或临时用 `HOST=0.0.0.0 node server.js` 起一次 —— 都写在
`说明.md` 的「已知限制」里。

### 验证

`e2e-gateway.cjs` 增加两项，直接看内核与成品：

- `ss -ltn` 显示服务只绑在 `127.0.0.1:<port>`（不是 `0.0.0.0`）—— 判据取自内核，不是读代码；
- 拆开成品 `.fpk` 的 `manifest`，断言**没有 `service_port`**、`platform = x86`。

---

## 2.4.7

### 修复：搜索框"可伸缩"做错了方向（顶栏仍然不合逻辑）

2.4.6 把搜索框改成 `flex-1`（会长大）—— 结果宽屏时它撑得很宽、反而把 logo 挤走。
用户指出正确的逻辑应该是：

> **全屏恢复最初的宽度，小窗口时挤压搜索栏，且不覆盖任何其他 UI。**

改成 `flex-1 max-w-[28rem]`：宽屏时顶到 28rem（就是最初那个宽度）就停住，多出来的空间
还给左右两段（logo 因此不会被撑走）；窄屏时按比例被压缩 —— **先挤搜索框**。

两种错误写法都试过、也都写进注释了，免得下次再走一遍：

- 只写 `flex-1`：会长到很宽，挤 logo ✗（2.4.6 就是这个）
- 只写 `flex-[0_1_28rem]`：永远 448px 不肯缩，窄屏时把右侧的打赏按钮顶到别的控件上 ✗

### 验证：宽度扫描

`e2e-narrow.cjs` 增加**宽度扫描**（900 / 1024 / 1200 / 1366 / 1440 / 1600 / 1920），
每个宽度量：搜索框宽度、logo 文字宽度、logo→筛选→搜索框→打赏→右侧控件是否两两重叠、
顶栏是否横向溢出、按钮文字是否显示：

| 视口 | 搜索框 | logo | 结果 |
| --- | --- | --- | --- |
| 1920 / 1600 | **448px（原宽）** | 104px | 零溢出、零重叠、文字显示 |
| 1440 | 448px（原宽） | 104px | 零溢出、零重叠 |
| 1366 | 429px（开始压缩） | 104px | 零溢出、零重叠 |
| 1200 / 1024 / 900 | 373 / 315 / 273px | 104px | 零溢出、零重叠、按钮仅图标 |

**logo 在从 1920 到 900 的每一个宽度下都稳定是 104px** —— 不再被挤。

### 测试自身也修了两处

- **不要量块级元素的宽度当"logo 宽度"**：`h1` 是块级元素，宽度等于所在容器的内容宽度，
  直接量它永远"很宽"，会误报成"logo 压到筛选按钮"（第一版扫描就是这么假报警的）。
  改用 `document.createRange()` 量**文字范围**。
- 旧断言「窗口变宽时搜索框跟着变宽」是按"越宽越好"写的 —— 与用户要的逻辑相反，
  改成验两个端点：窄屏 < 原宽、宽屏 = 原宽。

---

## 2.4.6

### 修复：接入网关后顶栏"挤在一起"

网关形态下应用跑在平台界面的 iframe 里，可用宽度比自己开浏览器窗口窄得多，
原来的顶栏布局是按"宽窗口"排的，一窄就互相挤压。

按反馈改四处（**打赏按钮按要求保持原样，没动**）：

1. **筛选/排序/主题色按钮：窄屏只留图标。** 按钮里的文字（`筛选` / `筛选 N`、
   排序字段名、`主题色`）与两条分隔线在 `2xl`（≥1536px）以下隐藏，
   只保留 Filter / 排序方向 / Palette 三个图标 —— 鼠标悬停时 `title` 仍然写全信息。
2. **搜索框改成可伸缩。** 原来写死 `w-[28rem]`，窄窗口下它不肯让位，两边就被挤爆。
   现在中段是 `flex-1 min-w-[10rem]`，随窗口伸缩；同时把左右两段改成按内容宽度
   （原来三段都是 `flex-1`，会互相抢空间）。
3. **打赏按钮不动。** 位置仍是"挂在搜索框右侧"，但中段右侧预留了它的宽度
   （`pr-[6.5rem]`）—— 它是绝对定位，不留位置在窄屏会压到右侧控件上。
4. **缩略图大小改成点图标弹出的滑块。** 常驻的滑块 + 数值标签要占近 200px 宽度，
   而这是低频操作。现在点 `Sliders` 图标弹出（`ThumbnailSizePopover`），
   点外面或 Esc 关闭，交互与顶栏其它弹层一致。

### 验证

新增 `e2e-narrow.cjs`（18 项）：把视口设成 1080px 模拟平台 iframe，逐项量几何 ——
按钮文字全部隐藏且宽度收到图标级、**顶栏零横向溢出**、筛选按钮没压到搜索框、
搜索框没压到右侧控件；打赏按钮仍是图标 + 文字且位置不变；
窗口从 1080 → 1700 时搜索框从 594px 长到 1174px（证明确实可伸缩）；
滑块默认不在页面上、点图标才出现、拖动生效、点外面关闭。

顺带修了两处**测试自身**的问题：
- 依赖滑块的用例（`e2e-slider`、`e2e-single-row`）要先点开弹层，已在 `harness.cjs`
  里加了公共助手 `ensureThumbnailSlider`；
- 弹层关闭要用**真鼠标事件**测：项目里的弹层统一监听 `mousedown`，
  用 `element.click()` 只会派发 `click`，会把"能关掉"误判成"关不掉"。

---

## 2.4.5

### 修复（重要）：桌面图标打开是白屏

用户反馈："点击桌面图标，打开是空白的，没有 ui" —— 直接访问 `http://nas:15002` 正常，
只有走网关的桌面入口白屏。

**根因**：桌面入口的地址是 `/app/niupic`（**没有结尾斜杠**），而前端产物里的资源是
相对路径 `./assets/index-xxx.js`。浏览器按"最后一段是文件"的规则解析相对地址，
把基准目录算成了上一级 —— 于是资源被请求到 `/app/assets/…` → 404 → **整页白屏**。
（带斜杠的 `/app/niupic/` 反而正常，所以很容易只在网关形态下才暴露；
直接开端口访问时 pathname 是 `/`，也一直正常。）

**修法：服务端注入 `<base>`**（《飞牛 fpk 开发规范》§6.4 推荐的做法）——
由后端渲染 `index.html` 时按请求前缀写入 `<base href="/app/niupic/">`，
把相对地址的基准固定下来。这样无论浏览器地址有没有结尾斜杠、平台会不会加/去斜杠，
资源都能正确解析。同时把 `express.static` 的 `index` 关掉 —— 否则它会绕过注入直接吐出原版 HTML。

**我这次的教训**：上一版写网关用例时只测了**带斜杠**的地址，正好漏掉真机平台给的那个形态 ——
"测了网关"不等于"测了真机上那个 URL"。现在 `e2e-gateway.cjs` 两种形态都测，
并且用**真浏览器**断言"页面有内容、不是白屏、没有 4xx 静态资源"。

**应急**：装的是 2.4.4 时，用 `http://<NAS>:15002` 打开即可（端口是刻意留的过渡后备）。

---

## 2.4.4

### 变更：接入飞牛统一网关（上架要求第 2 条）

上架要求写明"**鉴权优先接入飞牛统一网关，且不要监听公网端口**"。这一步把入口从
端口形态切到网关形态：

- `app/ui/config` 改为 `type: iframe` + `gatewayPrefix: /app/niupic` +
  `gatewaySocket: app.sock` + `url: /app/niupic` —— 桌面图标走平台网关，
  复用飞牛的登录态，不再需要用户记端口号；
- 应用同时监听 **Unix socket**（`<appdest>/app.sock`，权限 600，属主为应用账号），
  平台网关就是连它；
- **端口保留作过渡后备**：网关转发要在这台机器上由你点一次桌面图标才算验通，
  在验通之前把端口收掉会有"打不开应用"的风险。你确认没问题之后我再发一版去掉
  `service_port` 并只监听 `127.0.0.1`，那一步才是彻底的"不监听公网端口"。

### 修复：网关形态下会踩到的三个真问题

切网关不是改个配置就完事 —— 挂在 `/app/niupic/` 之后，**所有绝对路径都会指错地方**。
这一版把三处补齐，并写了 `e2e-gateway.cjs`（在本地起一个模拟平台网关的反向代理）
逐项验证：

1. **前端四处各写一遍基路径**（接口地址、图片 URL、复制图片、socket.io 路径）。
   统一到 `src/utils/appBase.js` 一处计算 —— 漏掉任何一处都表现为"图片加载失败"，
   而现象完全看不出是路径问题。
2. **socket.io 在网关下连不上**（真机实测的坑）：engine.io 在 attach() 时会把服务器上
   **已有的** request 监听器摘下来缓存，先按自己的 path 判断、不匹配才回头调用它们。
   于是"剥网关前缀"的监听器在 socket.io 请求上**根本没机会执行**，
   请求落到 Express 拿到 index.html —— 界面能打开、实时连接一直连不上、日志里一条
   socket 记录都没有。修法：在 `server.emit` 这一层归一 URL，任何监听器（含 engine.io
   接管的那些）看到的都已经是没有前缀的路径。
3. **Unix socket 链路上 Express 抢在 socket.io 前面应答**：TCP 那条链路顺序是对的
   （io 先于 app），socket 那条写反了 —— 同一条链路上的两个入口顺序不一致，
   只有走网关时才暴露。

### 变更：应用介绍里"完全免费 / 无付费内容 / 打赏自愿"前移

上架要求里"付费内容要在介绍首行声明"针对的是付费内容，本应用没有付费内容；
但审核方看到打赏入口可能会问，所以把这句声明从介绍末尾挪到**第二句**（紧跟"100% 开源免费"）。

### 验证

- 新增 `e2e-gateway.cjs`（15 项）：模拟平台网关（保留前缀转发 + 支持 WebSocket 升级），
  真浏览器从 `/app/niupic/` 打开，断言页面可开、资源相对路径、接口通、**图片全部显示**、
  socket.io 握手成功、Unix socket 直连可用、桌面入口声明的是网关形态。
- 全量回归 14 套（含新的网关套件）。

---

## 2.4.3

### 修复（重要）：升级到 2.4.2 后"图片全部加载失败"

**这是我 2.4.2 引入的回归**，用户反馈"图片全部显示加载失败，全量扫描也无法解决"。
不是缩略图的问题，是鉴权的问题 —— 全量扫描当然没用。

**根因**：2.4.2 把图片接口从免鉴权改成要登录，凭据走**会话 Cookie**。
但 Cookie 只在"登录 / 设置口令"那一步下发 —— 而老用户的登录动作发生在**升级之前**，
浏览器里只有 localStorage 里的 token（`<img>` 请求带不上 Header），
于是升级后所有图片请求都是 401。表现出来就是"图全挂了，重扫也没用"。

**修法：让会话自愈**。凡是"凭据有效、但这次请求没带 Cookie"的情况，服务端顺手补发一次 Cookie：

- `/api/auth/status` —— 页面加载后第一个被调用的接口，在这里补发最及时；
- 鉴权中间件 —— 覆盖"先调接口、后取图片"的其它路径。

这样老会话在下一次接口调用时就补全了，**用户不用重新登录、也不用重扫**。

**应急办法**（已装 2.4.2 未升级时）：在应用里退出登录再登录一次即可 ——
那一步会下发 Cookie，图片立刻恢复。

**回归用例**：`e2e-security.cjs` 新增一节"老会话自愈"——清空浏览器全部 Cookie、
只留 localStorage 的 token，刷新后断言图片仍然正常显示且服务端补发了 Cookie。
这类"升级路径"的问题不写用例，下次换个形式还会回来。

---

## 2.4.2

### 安全（重要）：修掉两个上架阻断级问题 + 一个越权隐患

上架自查时在**真机上实测**发现的，不是静态分析猜的。三个问题都出在"鉴权"和"路径"上：

**① 图片接口完全免鉴权（谁都能下载全部照片）**

`/api/image/thumbnail/*` 与 `/api/image/original/*` 被放在鉴权中间件的免鉴权白名单里。
实测：任何能访问 15002 端口的设备，**不用登录**就能把库里所有原图拉走。
（为什么当初会这么写：界面里的图片是 `<img src="/api/image/...">`，这种请求带不上
自定义 Header，于是开发者干脆把这两个接口设成公开。）

**② 路径穿越：能读库外任意文件，还能拿到应用自己的密钥**

那两个路由把用户传入的路径直接 `path.join(library.path, 用户输入)` 后 `sendFile`，
`..` 一路向上就能出库。实测（只读探测）：

```
GET /api/image/original/<库id>/../../../../etc/passwd
  → HTTP 200，4870 字节，读到 root:x:0:0:...

GET /api/image/original/<库id>/../../@appdata/niupic/config.json
  → HTTP 200，泄露 jwtSecret 与 passwordHash

用泄露出来的 jwtSecret 签一个 token → GET /api/library 返回 200，免密列出全部素材库
```

**③ 未设置口令时放行一切（fail-open）**

中间件里写着"如果没有设置密码，直接放行"。新装用户**还没设口令的那段时间**，
局域网内任何人可以调所有接口，包括删除、移动文件。

### 怎么修的

- **新增 `backend/src/utils/safePath.js`**：所有"用户给的相对路径"统一走 `resolveInside()`。
  用 `path.relative` 判断包含关系（不是字符串前缀 —— `/lib2` 这种同前缀兄弟目录会骗过前缀比较），
  拒绝绝对路径 / 盘符 / NUL / 反斜杠写法，并且**对已存在的部分做一次 `realpath`**，
  库里的软链接指向库外同样拦住。**FileService 里 14 处、图片两个路由、上传目标目录全部接上。**
- **图片接口不再免鉴权**：登录/设口令时下发 **HttpOnly + SameSite=Lax 的会话 Cookie**
  （鉴权中间件同时接受 `Authorization: Bearer` 与 Cookie）。浏览器里 `<img>` 会自动带上
  Cookie，所以接口可以和其他接口一样要求登录，前端**一行 URL 都不用改**。
- **fail-closed**：未设口令时只放行 `/api/auth/status`、`/api/auth/setup`、`/api/auth/login`、
  `/api/auth/logout`、`/api/health`，其余一律 403 并回 `PASSWORD_NOT_SET`；
  前端据此继续弹"设置口令"。
- **会话状态以服务端为准**：`/api/auth/status` 增加 `authenticated` 字段（认 Cookie），
  否则"有 Cookie、但 localStorage 没 token"的浏览器（换设备、清过缓存）会被误弹回登录页。
- **新增 `/api/auth/logout`**：退出登录时清掉服务端 Cookie（以前只清 localStorage，
  退出后图片 URL 仍然能直接打开）。
- **静态资源找不到就 404**：带扩展名却 404 的请求**不再回落 index.html**
  （否则前端报语法错误、图片变破图，排查时看不出是 404）。
- **网关就绪**：新增 Unix socket 监听（`<appdest>/app.sock`，权限 600，对应 `app/ui/config`
  的 `gatewaySocket`），HTTP 侧会剥掉 `/app/<appname>` 前缀，前端资源改成相对路径、
  API 与 socket.io 路径跟随网关前缀 —— 为切到飞牛统一网关做好准备（切换只需改
  `app/ui/config` 一处，待真机验证网关转发后再切）。

### 其它

- **manifest 去掉 `arch` 字段**：平台认识的 45 个字段里没有它（`platform = x86` 才是有效声明）。
- **应用介绍补打赏声明**（上架要求：付费内容要在介绍里声明"与飞牛官方无关"；
  打赏不属于付费内容，但要说清区别）：`desc` 末尾增加
  "本应用完全免费、无任何付费内容；界面内的打赏入口纯属自愿、不影响任何功能，与飞牛官方无关。"

### 验证

- 新增后端单测 `test/safe-path.test.js`（9 项）：`..` 各种写法、绝对路径、盘符、NUL、
  同前缀兄弟目录、库内软链逃逸、新建路径放行。
- 新增端到端 `e2e-security.cjs`（29 项）：未登录取图 401、五种穿越写法 403、
  只用 Cookie 也能看图、未设口令时数据接口 403、静态资源 404 不回落、
  网关前缀与 Unix socket 两条链路、socket 权限 600。
- **实测踩到的坑记一笔**：用 `fetch()` 测穿越会得到"200"—— WHATWG URL 会先把 `../`
  规范化掉，请求根本没到服务端。测穿越必须用 `curl --path-as-is` 或原始 HTTP 客户端。

---

## 2.4.1

### 变更：更换收款码素材，并把它压到原来的 1/14

用户给了新的收款码原图（两张 1024×1024 的 JPEG，界面清爽、没有截图边框）。换图时顺手
把压缩做对了：

- **发现**：新图是 JPEG，直接转 PNG 会把压缩噪点全带上 —— 600×600 各要 **106KB / 91KB**，
  base64 之后前端包要胖 260KB，而界面里实际只显示 180px。
- **做法**：试三种编码（二值化 2 色 / 灰阶 64 色 / 调色板 256 色），**逐个解码验证**，
  取通过验证里最小的那个；选中后再把它缩到**界面显示尺寸 180px** 解一次码 ——
  显示出来还能扫，才算过。最终二值化版本 **7.3KB / 6.3KB**（原来的 1/14），
  三种候选都能扫、180px 下也都能扫，只是二值化最小。
- **顺带**：做了这一步之后，前端包反而比 2.4.0 **小了约 100KB**（2.4.0 内嵌的是
  未经优化的 600×600 PNG）。

素材工具也补了两处稳健性：原图文件名/格式不再写死（第一版是 `微信.png`，这版两张都是
`.jpg`）；解码时统一把 1/3/4 通道补成 RGBA（二值化后的 PNG 是 1 通道，
`ensureAlpha()` 不保证给 4 通道，直接喂给 jsQR 会报 "Malformed data"）。

**收款去向没有变化**：新旧图解码出来的内容**逐字符相同**（微信 74 字符、支付宝 45 字符），
换的是图片本身，不是收款账号。若本意是换账号，需要重新导出收款码。

### 文档更正：把"从哪个版本开始接手"写准

原 README 写的是"自 2.0.x 起接手开发与维护"，这与事实不符，容易让人以为 1.3.x 是原作者做的。
核对后的事实（依据：上游公开仓库的 43 个提交全部由原作者在 2025-12-01 ~ 12-07 完成，
版本到 **1.2.0**，仓库 2026-03 归档；以及本机恢复出来的 2026-09-10 工作区会话记录里
明确写着 `manifest 1.2.0 → 1.3.0` 与产物 `flypic_1.3.0_x86.fpk`）：

- 原作者 ZangXincz 发布并维护到 **1.2.0**（2025-12）；**MIT 声明覆盖的是这部分代码**；
- **1.3.0 起由 LiuFudi 维护**（1.3.0 在 1.2.0 基础上做，仍沿用 FlyPic 名字），
  之后更名牛图 NiuPic，版本从 2.0.x 起 —— 这些都归 LiuFudi。

已改 `README.md`（接手说明）与 `NOTICE`（补"上游代码范围"一节）。**只改文档，不动版本号**：
两份文件都不在安装包里，成品 `.fpk` 一个字节没变，SHA-256 仍是
`1779be73ae16992293dfd66ab335dcd46a1a861c27d9a0cc5dc0cf0c5c4de92c`；
变的是源码包（README/NOTICE 本来就在源码里）。

---

## 2.4.0

### 新增：打赏入口（顶栏搜索栏右侧）

界面顶栏搜索栏右侧多了一个 ❤ 打赏入口，点开是微信 / 支付宝两个收款码与一句说明。
按《打赏功能规范》实现，几条是刻意的约束，改之前先看一眼：

- **纯本地**：不联网、不上报、不统计点击、不加载远程图片。收款码是构建期内嵌的
  data URI，断网照常显示；
- **绝不自动弹出**：只有点了入口才出现；关闭只需一次点击（× / 点遮罩 / Esc），
  没有"再想想"式的挽留；
- **入口始终显示**，程序里不提供关闭它的开关。这条只约束行为，**不写进任何面向用户的
  文案** —— 规范里说得很清楚："关不掉"是对用户的宣告，哪怕无害也会被读成"你得忍受它"；
- **与功能完全解耦**：不做任何"是否打赏过"的判断，没有付费才能用的功能，也没有打赏后解锁；
- **不碰支付**：只显示收款码，转账在微信/支付宝里完成，应用不处理任何支付信息。

文案写在 `frontend/src/config/donate.json`（含 `enabled` 开关，供分发者在源码层面整体关闭）。

### 新增：收款码构建期内嵌

`assets/donate/{wechat,alipay}.png` → `scripts/make-donate-qr.js` → `frontend/src/assets/donateQr.js`
（生成物进仓库，直接 clone 也能构建）。

**为什么不放一张图片文件**：包内、以及安装后的应用目录里，一个独立图片文件拆包覆盖一张
就能换成别人的收款码，一行代码都不用改 —— 用户以为在支持原作者，实际打给了别人。
内嵌之后想换必须改代码重新构建，那已经等于自己 fork 一份，受开源协议约束。
**边界也说清楚**：这只堵住"换图片"这条路；开源协议下没有任何技术手段能阻止别人
改代码后重新构建，真正的约束是协议本身（GPL-3.0 §5 要求修改版显著标注已修改）。

原图按规范处理过：正方形、600×600、四周留白（静区）、浅底深码，裁剪后**重新解码验证**
确认还能扫出来（不是按比例估着裁的）。

### 变更

- **版本号 2.3.10 → 2.4.0**：按《开发规范》1.1，向下兼容地新增功能是 MINOR，
  不再按补丁号发。
- **打包脚本构建前重新生成内嵌收款码**：否则会发出一个"包内的码"和 `assets/donate/`
  对不上的版本 —— 这类不一致肉眼看不出来。
- **打包脚本清理名单补齐**：`node_modules/.package-map.json` 与 `node_modules/.pnpm/`
  也是 pnpm 的记账文件（`node_modules` 里 113 KB），运行时并不读它们。
  这一条来自《飞牛 fpk 开发规范》9.6 的实测记录 —— 逐文件比对没发现，
  是照着规范复查才补上的。

---

## 2.3.10

**本版没有功能改动。** 这一版做的是"把项目的法律与交付底子补齐"：换协议、给每个
源文件加许可标识、让用户拿到安装包就能看到协议、把发版流程里几处靠人记的地方
改成机器校验。功能与 2.3.9 完全一致，可以放心升级。

### 变更（重要）：许可证从 MIT 换成 GPL-3.0-or-later

**为什么换**：MIT 允许任何人拿这份代码闭源打包进商业产品。牛图是在别人的工作
（FlyPic）上继续往前做的，也应该把这份自由传下去 —— 换成著佐权协议后，
分发修改版的人必须同样开源。项目组统一用 GPL-3.0-or-later，本项目跟随。

**这件事不可撤回，也必须说清楚**：

- 已经按 MIT 发布的版本（**2.3.9 及更早**）继续是 MIT。从那些版本拿到代码的人，
  原有权利**永久有效**，不会被这次变更收回 —— 换协议只对**新版本**生效。
- 上游 FlyPic 的 MIT 声明完整保留在 [NOTICE](NOTICE) 里（MIT 要求随分发保留），
  LICENSE 则是 FSF 的 GPL-3.0 逐字原文（35149 字节，SHA-256
  `3972dc9744f6499f0f9b2dbf76696f2ae7ad8af9b23dde66d6af86c9dfb36986`）。
- MIT 与 GPL-3.0 兼容（MIT 允许再许可为 GPL），因此"整体以 GPL 分发、同时保留上游
  MIT 声明"是合规的。

### 新增

- **每个源文件带 SPDX 头**（`SPDX-License-Identifier: GPL-3.0-or-later` +
  `Copyright (C) 2026 LiuFudi`）：148 个源文件全部覆盖（其中 146 个是脚本自动补的），
  含后端、前端、构建脚本、fnOS 生命周期钩子与官网页面。用 SPDX 标识符而不是自由文本，
  才能被 REUSE、扫描器和 GitHub 识别。
- **`scripts/check-spdx.js`**：发版前扫一遍谁漏了头，`--fix` 可自动补。**已接进打包脚本**，
  缺头直接终止构建 —— 这类"新增文件时忘记加"的事靠人记一定会漏。
- **协议全文随包分发**：打包时把根目录 `LICENSE` 复制到 `app/ui/LICENSE.txt`（fnOS
  应用资源目录）与 `server/public/LICENSE.txt`（后端静态托管，浏览器可直接打开
  `/LICENSE.txt`）。一份真源，两处落地，不是各写一份。
- **界面「关于」页**（侧栏「素材库」标题右侧的信息按钮）：显示版本号、版权、协议与
  协议全文链接、作者主页、上游项目、**商标与非隶属声明**、免责声明。用户装完就能看到
  自己拿到的是什么协议，不必去翻仓库。版本号由 Vite 在构建时从根 `package.json` 注入
  （`__APP_VERSION__`），**不在前端另存常量**。
- **`NOTICE`**：版权、上游 MIT 原文、商标与非隶属声明、免责声明、第三方依赖与素材说明。
- **`SECURITY.md`**：漏洞走私密渠道报告（附这个应用真实攻击面与"什么不算漏洞"），
  部署建议（不要把 15002 直接映射到公网）。
- **`CONTRIBUTING.md`**：提交即同意以本项目协议授权（inbound = outbound）、必须带
  SPDX 头、测试命令、"不会接受的改动"。
- **`.github/FUNDING.yml`**：GitHub 赞助入口。

### 变更

- **运行时依赖树加锁**（`scripts/server-deps/pnpm-lock.yaml`，进仓库）：
  之前服务端依赖是用 `pnpm install` 现场解析的，`backend/package.json` 里写的是
  `^11.10.0` 这种范围，**transitive 版本会随上游发版漂移** —— 同一个源码包，
  今天构建和下周构建装出来的依赖可能不是同一棵。等于把"上游什么时候发了新版"的
  随机性带进了成品，交付物也就无法复现。现在锁文件跟着源码走，构建用
  `--frozen-lockfile` 安装；改了 `backend/package.json` 却没更新锁文件时，
  构建会直接失败（用 `node scripts/build-fpk.js --update-lockfile` 更新）。
  **这是干净目录重建验证时发现的**：重建出来的包比交付的小 49 KB，逐项比对后
  差异全部落在 `node_modules` 里（`@types/node` 22.20.1 → 26.6.2、
  `brace-expansion` 1.1.18 → 1.1.21），应用自身的文件一个字节都没差。
- **打包脚本交叉校验版本号**：`niupic/manifest`、根 `package.json`、`CHANGELOG.md`
  三处必须一致，不一致直接报错退出（`build-fpk.js` 开头）。以前全靠人记，漏改一处就会
  发出一个"装上去是 2.3.10、关于页显示 2.3.9"的包。
- **README**：补许可、商标与非隶属声明、免责声明、安全与参与开发章节；徽章加许可证。
- **`package.json` 的 `license`** 字段 `MIT` → `GPL-3.0-or-later`（不止是文本，机器可读
  的字段也要跟上，扫描器看的是它）。
- fnOS 应用清单的 `changelog` 摘要只保留最近若干版本，完整历史以本文件为准。
- **源码包里只放与软件源码有关的东西**：`开发纪要.md`、`发布到GitHub指南.md`、
  `牛图打包部署指南.md` 这类自用文档不再随源码包分发（它们继续留在仓库里，带 git 历史、
  可追溯）。理由很简单 —— 下载源码的人是要复现这个软件，不需要我的开发过程记录和
  个人操作指南；判断标准就是"我要复现这个软件需不需要它"。这一条已写进《通用开发规范》2.2.1，
  打包脚本按排除清单执行（这些文件是 git 跟踪的，光靠 `.gitignore` 挡不住）。

### 安全

- 交付前对发布树与解开的成品做全局扫描：主机专属绝对路径、内网 IP、主机名、
  口令与令牌、临时文件（`.supw` / `runas.py` / `root.sh` 之类）一律不得出现。
- **成品里不再带构建机路径**：`node_modules/.modules.yaml` 与
  `.pnpm-workspace-state-v1.json` 是 pnpm 的记账文件，里面记着构建机的
  `storeDir` 与工作目录绝对路径。运行时根本不读它们（`require` 只看真实目录），
  留着等于把构建机的目录结构随成品发出去，现在打包时直接删掉。
  —— 这一条也是"解开成品扫描主机信息"扫出来的，肉眼根本不会去看 node_modules。
- **顺手清理了真实的素材库名与主机路径**：代码注释、测试夹具与文档里出现过
  "某个库叫 xx、路径里带某个大写目录"这类来自排查现场的真实信息 —— 这些会随源码包
  发到所有人手里。现在统一换成通用示例（`/vol1/1000/Photos`、`Photos-Backup/Camera`），
  技术含义不变（大小写、连字符这些触发条件都保留）。发布说明里的路径也统一成
  `/vol1/...` 并注明"卷号以你的 NAS 为准"。历史条目按"只增不改"保留原文。

---

## 2.3.9

### 修复：图片突然变成「暂无图片」—— 真凶不是筛选

2.3.8 我以为是"筛选条件跨库残留"，改完用户说还是不行。这次直接在**用户的实机上**
（用无头浏览器打开真实运行的 15002 端口）复现并抓到了真凶：

```
+0ms    22 imgs, 100/15249, ok         ← 相机
[58169] REQ  library/色图/validate
[58228] REQ  image?libraryId=相机 ...   ← 切换过程中的中间态请求
+800ms  27 imgs, 100/6269, ok          ← 色图的第一页加载完成
…
socket 消息: 42["scanComplete",{"libraryId":"1789455094724","results":{"added":10}}]
[8338] REQ image/folders?libraryId=色图
[8339] REQ image/stats?libraryId=色图
+1600ms 0 imgs, 0/6269, EMPTY          ← ★ 被清空了
```

**后台一次扫描结束，把网格清空了。** 看 `App.jsx` 里 `scanComplete` 的处理：

```js
// 如果没有选中文件夹且没有搜索条件，不加载图片（保持在 Dashboard）
(imageState.selectedFolder || imageState.searchKeywords || imageState.filters.formats.length > 0)
  ? imageAPI.search(libraryId, params)
  : Promise.resolve({ images: [] }),     // ← 直接把 images 清空
```

这个"Dashboard 分支"的假设是"没选文件夹 = 用户还在首页"。但**扫描随时可能结束**：
文件监控（lightweightWatcher）发现新文件就会自己跑一次增量同步，然后广播 `scanComplete`。
于是用户在"全部图片"下正常看图，后台一次扫描完成就把列表清空了 ——
「图片短暂显示一会，然后暂无图片」；点文件夹走的是另一条加载路径、刷新则没有这个事件，
所以两种办法都能"修好"。

**为什么它看起来是"切库"触发的**：切库时前端会清一次列表再加载，刚好和后台扫描完成的时机
撞在一起（尤其大库在机械硬盘上，扫描/同步长时间在跑），所以用户感觉"一切库就空"。

修法：扫描完成时**按当前筛选条件重新加载第一页**（folder / keywords / filters / sort 都带上），
不再有"清空"这个分支；顺带把 folders / originalImages / 统计和分页状态一起刷新。

回归测试：`e2e-switch-library.cjs` 新增一节 —— 触发一次真实的增量扫描（`wait:false`，
服务端会广播 `scanComplete`），断言网格还在、数量还对。**把修复撤掉重跑，会明确报
`0 张 / 暂无图片`**，确认它抓得住。

顺带补了一条静态检查（`src/utils/undefined-symbols.test.js`）：
"用了某个跨模块符号，但既没定义也没 import"。修这个 bug 时我顺手写漏了
`buildImageQueryParams` 的 import —— 构建不报错、单测不报错，只有跑起来才白屏。
这是本项目第四次同类问题了（前三次：`filterOptions`、`useMemo`、现在的它）。

### 新增：两个"省时间"的开关（大库友好）

用户反馈：**相机库 15000 多张、机械硬盘，全量重扫要 30 分钟以上。**

**1. 全量扫描跳过没改动过的文件。**

原来对每个文件都 `processImage(force)` —— 重读元数据 + 重画缩略图。现在先比
"大小 + 修改时间"的快速哈希：

| 情况 | 行为 |
|---|---|
| 记录在、哈希没变、缩略图文件也在 | **跳过**（完全不碰图片内容） |
| 缩略图丢了 | 只补这一张的缩略图 |
| 大小/修改时间变了 | 重新读元数据 + 重画缩略图 |

绝大多数文件属于第一种，所以大库重扫从"半小时"变成"几秒 ~ 几分钟"（取决于有多少变化）。
真要看"每个文件都硬重读一遍"的行为，代码里还留着 `{ skipUnchanged: false }`。

**2. 「修复文件夹结构」—— 不读磁盘，按数据库重建，几秒完成。**

专门解决"重扫之后文件夹显示 0 张"（以及磁盘上挪过文件夹想对齐）。数据库里
`images.folder` 已经写清楚每张图在哪个目录，直接从它推出文件夹层级和每层张数即可，
完全不用碰图片文件。

顺带修了一个隐藏很久的死代码：这个接口（`POST /api/scan/fix-folders`）原来调的是
`scanner.fixFolderPaths`，而 **scanner 里根本没有这个函数** —— 点了就是 500。

### 验证

- 新增后端测试 `test/rescan-fast.test.js`（5 项）：跳过逻辑、改动重扫、缺缩略图补生成、
  `fixFolderPaths` 重建与修复 0 计数
- `e2e-switch-library.cjs` 扩到 20 项（新增"后台扫描结束不能清空网格"）
- 新增前端静态检查 `undefined-symbols.test.js`

全量回归：前端 101 + 后端 55 + 生命周期 19 + 十套端到端
（33/50/27/14/23/8/16/92/54/20）= **512 项全过**。

---

## 2.3.8

### 修复：切换素材库后变成「暂无图片」

用户报的长期 bug：**切换库后图片短暂显示，随后提示「暂无图片」；点一下文件夹或刷新又正常。**

复现与定位（先在本地造了两个库，A 全是 jpg、B 全是 png）：

1. 在 A 库打开筛选面板，选中「只看 jpeg」→ 请求里带上 `formats=jpeg`，A 正常显示
2. 切到 B 库 → **`imgs: 0`、页面显示「暂无图片」**，网络里那条取图请求仍然带着 `formats=jpeg`
3. 点一下文件夹（或刷新）→ 又好了

根因：**筛选条件和搜索词是"上一个库"的语境，却会跨库残留。**

```js
// Header.jsx —— 只跟了文件夹变化
useEffect(() => { resetFilters(); }, [selectedFolder, resetFilters]);
```

在 A 库选了「只看 jpg」，切到没有 jpg 的 B 库之后，B 也被这个条件筛了一遍 → 0 张 → 暂无图片。
而点文件夹会让 `selectedFolder` 变化、触发这条 effect → 筛选被清掉 → 图片回来了；
刷新页面同理（筛选不持久化）。**三个现象全都对得上**，也就解释了为什么它"稳定复现"又"点一下就没事"。

修复两处：

- `Header.jsx` 的清筛选 effect 加上 `currentLibraryId`（切库也清）
- `Sidebar.jsx` 的切换流程里，清图片/文件夹的同时显式 `resetFilters()`（更直白，也覆盖"当前没选文件夹"的情况）

回归测试 `e2e-switch-library.cjs`（15 项）：造 A(jpg)/B(png) 两个库，在 A 里开筛选 →
断言请求里确实带了 `formats=` → 切到 B → 断言有图、数量是 B 自己的、**等 5 秒仍然有图** → 再切回 A 也正常。
把修复撤掉重跑，这条测试会明确报 `0 张 / 暂无图片` —— 确认它抓得住这个 bug。

### 修复：全量重扫之后文件夹的文件数都是 0

用户报的第二个 bug：**全量重扫相机库之后，总张数和图片都正常，但文件夹显示的文件数是 0。**

根因在 `rescanLibrary` 的收尾：

```js
// 重扫末尾重建文件夹结构 —— insertFolder 是 INSERT OR REPLACE，
// 撞 path 就删了重插，新行的 image_count 是 0
const folders = await getFolderStructure(libraryPath);
db.db.transaction((list) => list.forEach((f) => db.insertFolder(f)))(folders);
// ← 这里少了 updateAllFolderCounts()
```

全量扫描（`scanLibrary`）是在**图片入库之后**才 `updateAllFolderCounts()` 的，所以一直正常；
重扫把文件夹重建放在最后，重建完却没重算 → 一片 0。

修复：重建结构之后立刻 `db.updateAllFolderCounts()`。

顺带一个好消息：用户装上新版之后，相机库这次扫出了 **15249 张**（之前是 0）——
2.3.2 修的扫描问题在真机上确认有效。

### 验证

新增 `e2e-switch-library.cjs`（15 项）；`e2e-scan-camera.cjs` 扩到 **54 项**，新增一节
「重扫之后文件夹计数不能是 0」（断言至少有一个文件夹非 0，且顶层文件夹把子文件夹也算进去）。

全量回归：前端 100 + 后端 50 + 生命周期 19 + **十套**端到端
（33/50/27/14/23/9/16/92/54/15）= **502 项全过**。

---

## 2.3.7

### 调整：扫描菜单改成「范围 × 方式」两个维度

上一版并列了三个入口：**全量重扫**（当前库）、**全库重扫**（所有库）、**按库扫描**（选某个库）。
用户直接问：「全量重扫和全库重扫是不是一样的？」—— 名字只差一个字；
接着又问：「把范围塞进全量扫描里，那不就和按库扫描重复了吗？」

两个问题都问在点子上：**扫描其实是两个正交的维度**，我却把它们揉成了三个名字相近的入口。

```
扫描 / 刷新素材库
─────────────────────────────
扫描范围   [ 相机（当前） ▾ ]      ← 当前库 / 某个库 / 全部库
─────────────────────────────
[ 增量扫描 ]  只找新增和删除，很快
[ 全量扫描 ]  每个文件重读一遍（评分收藏保留）
```

- **范围**：一个选择器，默认「当前库」，可以选任意一个库或「全部库」
- **方式**：两个按钮，各自右侧标着当前范围（「全量扫描  相机」）
- 组合随便玩：想全量扫某一个库 → 范围选它 + 点全量扫描；想增量扫全部库也行
- 选「全部库」时挨个排上（库间隔 1.5 秒），选单个库时就发一个请求

三个旧入口的能力一个没少，但菜单里只剩两个动作，不会再有"这俩是不是一样"的疑问。

### 验证

`e2e-scan-camera.cjs` 扩到 **51 项**，这一节改成：

| 断言 | 结果 |
|---|---|
| 菜单里只有「增量扫描」+「全量扫描」两个动作 | `scan-menu-sync` / `scan-menu-full` |
| 没有并列的三个旧入口，也没把范围藏进全量扫描里 | 旧 testid 一个都不在 |
| 范围是独立选择器，默认「当前库」 | `相机（当前）` |
| 范围里能选任意库 + 全部库 | `相机（当前）｜第二个库｜全部库（2 个）` |
| 范围选某库 → 全量扫描只对那个库发请求 | 请求体里只有一个 libraryId |
| 范围选全部库 → 每个库都排上，且每个只排一次 | 2 次请求 / 2 个库 |

全量回归：前端 100 + 后端 50 + 生命周期 19 + 九套端到端（33/50/27/14/23/8/16/92/51）
= **483 项全过**。

---

## 2.3.6

### 新增：扫描菜单里的「按库扫描」和「全库重扫」

素材库不止一个的时候，想重扫某个库得先切过去再点扫描 —— 现在不用了：

```
扫描 / 刷新素材库
├─ 增量扫描                 只找新增和删除的文件，快
├─ 全量重扫（扫描库文件）    每个文件重读一遍 + 清理失效记录
├─ 按库扫描（2 个库）  ▾
│    色图                     [重扫]
│    相机（当前）              [重扫]
└─ 全库重扫（2 个库）        挨个把每个库都排上
```

- 每个库一行，各自一个「重扫」按钮；当前库标了「（当前）」
- 全库重扫**保留**：顺序把每个库都排上，库与库之间隔 1.5 秒，别一下子把磁盘 IO 顶满
- 重扫都是服务端异步任务：点完就能关菜单、切库、看进度条（走 Socket 推送）

### 调整：路径复制按钮挪到路径正下方

上一版把「Win / Linux」两个按钮放在路径**右侧**，长路径被挤成好几行。
现在改成路径一行、按钮一行：

```
路径:
/vol1/1000/Photos/IMG_0001.JPG
[ Linux 路径 ]  [ Windows 路径 ]
```

按钮文字也写全了（「Linux 路径」/「Windows 路径」，不再是 `Win` / `Linux`）。

### 修复：切进文件夹时白屏

改路径那两块 JSX 的时候，模板替换多留了一层花括号（`{{fullPath}}`），
React 直接抛 `Objects are not valid as a React child`，**整个页面白屏**。
是端到端测试抓到的（第 12 节之后全挂）。

顺带修了一个自己的竞态：扫描菜单里「全库重扫」用 `busy` 状态拦重复点击，
但 `setState` 是异步的，连点两下时第二次会在状态更新前溜进去 —— 每个库被扫两遍。
改成用 `useRef` 当闸门（这个也是端到端先报出来的，一开始还怀疑是测试记录器重复计数，
查下来两者都占了：产品有竞态，测试的记录器也被装了两次）。

### 验证

`e2e-scan-camera.cjs` 扩到 **47 项**，新增：

| 断言 | 结果 |
|---|---|
| 两个复制按钮在路径正下方（不是右侧） | 路径底 757 / 按钮顶 763 |
| 菜单里列出所有库、各自一个重扫按钮 | 相机 / 第二个库 |
| 只对选中的那个库发重扫请求，不顺带扫当前库 | 请求体里只有那一个 libraryId |
| 被选中的库真的扫出了文件 | total=1 |
| 全库重扫把每个库都排上，**每个库只排一次** | 2 次请求 / 2 个库 |

全量回归：前端 100 + 后端 50 + 生命周期 19 + 九套端到端（33/50/27/14/23/9/16/92/47）
= **480 项全过**。

---

## 2.3.5

### 新增：相机 RAW 支持

DNG / CR2 / CR3 / CRW / NEF / NRW / ARW / SR2 / RW2 / ORF / RAF / SRW / PEF / 3FR / IIQ /
MRW / X3F / ERF / KDC / MEF —— 这些是各家相机自己的格式，sharp（libvips）读不了，
所以之前它们：

- **没有缩略图**，网格里只有一张灰色占位图
- **宽高一律是 640×480**（占位尺寸），竖着拍的片子被当成横图排版

但相机一定会在 RAW 里塞一张 **JPEG 预览**（就是回放时屏幕上给你看的那张，
通常 1600×1200 到全尺寸）。新增 `utils/rawPreview.js` 把这张抠出来，缩略图和宽高就都有了，
不用依赖 libraw / dcraw（飞牛上不一定装得出来）：

1. **按 TIFF 结构走 IFD**（DNG/CR2/NEF/ARW/RW2 都是 TIFF 容器），
   找 `JPEGInterchangeFormat(0x0201)` / `Length(0x0202)` 这对标准标签；
   会跟着 `SubIFDs` 往里走，带环路保护
2. **结构不认识时扫最大的那段 JPEG**（CR3 是 ISO BMFF、RAF 是富士自己的容器），
   取 SOI(FFD8FF)…EOI(FFD9) 之间最长的一段

只读文件头部 64MB，不会把整个大文件读进内存；顺便支持大小端。

### 修复：视频封面不全（部分 mov / mp4 没有缩略图）

老实现有四个坑，正好凑成"有些视频没封面"：

| 问题 | 后果 |
|---|---|
| `exec('ffmpeg -version', {timeout: 2000})` | 系统忙时 2 秒不够 → 判成"没装 ffmpeg" → **所有**视频都没封面 |
| 固定取**第 2 秒**的帧 | 比 2 秒短的手机小片段取不到帧 → 没封面 |
| 输出侧 `-ss` + 10 秒超时 | 要解码到第 2 秒，4K 长片在 NAS 上经常超时 |
| `exec` 拼字符串 | 路径里有空格 / 引号 / 中文就挂（用户的库里这些都有） |

现在：

- `ffmpeg` / `ffprobe` 路径**探测一次并缓存**（先看环境变量，再试 `/usr/bin`、`/usr/local/bin`、
  `/usr/trim/bin`、`/vol1/@appstore/ffmpeg/bin`），探测超时给到 15 秒
- 用 **`execFile` + 参数数组**，不拼 shell 字符串
- 用 `ffprobe` 拿真实时长/宽高（只读头部，不解码），**按长度挑取帧时间点**：
  ≤1 秒取开头、≤3 秒取中间、更长的取 25% 处，逐个重试，最后兜底第 0 帧
- 输入侧 `-ss`（快）+ `scale=640:-2` 减少解码量，超时给到 30 秒

顺带修了一个更隐蔽的：**视频的宽高一直用的是"封面缩略图"的尺寸**。
`utils/scanner.js` 里 `useOriginalDims` 只对 `fileType === 'image'` 成立，
视频走的是 `thumbnails.width/height` —— 于是所有视频都被存成 640×480，
16:9 的片子在网格里比例是错的。现在视频也用 ffprobe 的真实宽高。

### 修复：文件路径显示成 Windows 写法

详情里的路径原来是**按浏览器所在系统**决定分隔符的
（`navigator.platform` 里有 `win` 就用反斜杠）。但这个路径是**服务器上**的真实路径 ——
用户用 Windows 打开网页，就显示成 `\vol1\1000\Photos\…`，粘到飞牛的终端里根本用不了。

现在：

- 界面**一律显示 Linux 写法**（`/vol1/1000/Photos/IMG_0001.JPG`）
- 复制改成**两个按钮**：`Win`（反斜杠写法）和 `Linux`（正斜杠写法），各复制各的
- 逻辑抽到 `frontend/src/utils/filePath.js`（`toLinuxPath` / `toWindowsPath` /
  `joinLibraryPath`），带单测

### 调整：筛选卡按 6 : 4 分栏

用户指定的比例：筛选 6、主题色 4。实测筛选 580px（60%）/ 主题色 386px（40%）。
两边各自有最小宽度（22rem / 16rem），真的放不下时整栏换行到下面 —— 而不是把卡片撑宽。

### 验证

`e2e-scan-camera.cjs` 扩到 **33 项**，夹具里放了：

- **真结构的 RAW**：把一张 1600×1200 的 JPEG 包进 TIFF 容器做成 RW2 / DNG / CR2
- **真视频**（系统 ffmpeg 生成）：0.4 秒的 MOV（老实现必然没封面）、3 秒的 MP4、1.2 秒的 MOV

断言：每个 RAW 都有缩略图且宽高 = 1600×1200（不是 640×480）；每个视频都有封面
且宽高 = 320×240（来自 ffprobe）；缩略图解码出来是 640×480 的真图（不是占位图）；
详情里的路径以 `/` 开头、不含反斜杠、有两个复制按钮。

新增后端单测 `raw-preview.test.js`（6 项）：TIFF 大小端、IFD 抠图、兜底扫描挑最大的那段、
没有 JPEG 的文件、不存在的文件、SOF 解析宽高。

全量回归：前端 100 + 后端 50 + 生命周期 19 + 九套端到端（33/50/27/14/23/8/16/92/33）
= **465 项全过**。

---

## 2.3.4

### 调整

**筛选和主题色改回横向排开。**

上一版为了消掉卡片里的横向滚动条，把主题色挪到了筛选条件下面（上下排）。
横向滚动是没有了，但版式不是想要的 —— 桌面端应该**横向排开**。

真正的原因不是"横排不行"，而是**当时横排用的是"完整版"的调色板**：
色轮 168px + 明度/十六进制 160px + 预设 6 列，一行要 700 多像素，
塞进 320px 的一栏里必然溢出。现在给调色板加了**紧凑版**：

| | 完整版 | 紧凑版（桌面端右栏） |
|---|---|---|
| 预设色块 | 8×8，间距 8 | 7×7，间距 6 |
| 色轮 | 168px | 132px |
| 内部排布 | 宽屏横排 | 一律竖排 |
| 明度/十六进制 | 固定 160px 宽 | 自适应填满 |

筛选卡内部：

```
┌─ 筛选（桌面端，卡片铺满图片区）─────────────────────────────┐
│ ┌─ 筛选条件（flex-1，最小 20rem）─┐ ┌─ 主题色（17rem）──┐ │
│ │ [只显示|排除]          清空     │ │ 预设 ●●●●●●       │ │
│ │ 格式 图片/视频/其他 + chips     │ │      ●●●●●●       │ │
│ │ 大小 [●━━━━━●] 刻度在下面       │ │ 色轮(132) 明度    │ │
│ │ 方向 / 评分                     │ │ 十六进制 / 预览   │ │
│ └─────────────────────────────────┘ └───────────────────┘ │
└──────────────────────────────────────────────────────────┘
```

放不下时（窗口很窄）**整栏换行到下面**，而不是把卡片撑宽 ——
这条用 `flex-wrap` + 左栏 `min-w-[20rem]` 实现，正是用户说的"挤压/换行但要合理"。

手机端保持不变（上下排）：手机上根本没有横向空间，横排只会两栏都挤成一条。
实现上是 App（桌面）传 `horizontal`，手机端不传。

**按钮上补上「主题色」字样。**

主题色既然并进了这张卡片，按钮就得写出来 —— 现在是
`[漏斗(角标)] 筛选 3 | ⇅ 文件名 ↑ | 🎨 主题色`，图标颜色也跟着当前主题色走。
手机端那个紧凑按钮放不下文字，加了个调色板图标（title 里也写了主题色）。

### 验证

`e2e-filter-mode.cjs` 扩到 **91 项**，新增：

| 断言 | 结果 |
|---|---|
| 桌面端筛选和主题色横向排开 | 筛选 285~959 / 主题色从 979 起 |
| 主题色那一栏宽度合理 | 272px（17rem） |
| 卡片仍然不需要横向滚动 | `scrollWidth - clientWidth = 0` |
| 手机端保持上下排 | 主题色 top 695 ≥ 筛选底 679 |
| 按钮上写明了「主题色」 | `"筛选 创建时间 主题色"` |

全量回归：前端 92 + 后端 44 + 生命周期 19 + 九套端到端（33/50/27/14/23/8/16/91/17）
= **434 项全过**。

---

## 2.3.3

装上 2.3.2 之后提的三条，都是版面/交互上的收尾。

### 调整

**卡片不再横向溢出，宽度就是图片区宽度。**

上一版为了"填满右侧空白"，把筛选条件和主题色并排成三栏。结果在用户的窗口里
**卡片内部出现了横向滚动条**（内容比卡片宽），要左右翻着看 —— 比原来的空白更难受。

现在改成上下排：筛选条件一整块，主题色作为下面一整行（`mt-4 pt-4 border-t` 分隔）。
卡片宽度仍然正好是图片区宽度（实测 998 / 1024px），但
`scrollWidth - clientWidth = 0`，**一点横向滚动都不需要**；这一点已经写成断言。

两张卡片的高度也收了一些（46vh / 52vh），给图片区多留出可视高度。

**文件大小：两个把手的区间滑块，刻度在下面。**

- 两个把手默认占两端 = 不限（用户要的"默认占据两端"）
- 拖左手把定下限、拖右手把定上限，区间内高亮
- **刻度从滑条侧面挪到滑条下面**：`< 1M  1-5  5-10  10-20  20-40  40-60  60-80  80-100  100+`，
  正对着各自的停靠点，区间内的刻度会跟着高亮
- 两个把手不许交叉：左手把拖到右手把右边会自动交换，不会出现 min > max
  （端到端里两个方向都验了）
- 左边读数是当前区间（不限 / `1 - 5 MB ~ 20 - 40 MB`），右边是这个区间里有多少张

**调色板按钮并入「筛选 + 排序」。**

顶栏原来还有一个单独的调色板按钮。现在去掉了 —— 主题色就在筛选卡片里，
一个按钮就够。（手机端「设置」里那份保留，手机上没有这个卡片入口的取舍问题。）

### 验证

`e2e-filter-mode.cjs` 扩到 **87 项**，这一轮新增：

| 断言 | 说明 |
|---|---|
| 卡片宽度 <= 图片区宽度 | 998 <= 1024 |
| **卡片内部不需要横向滚动** | `scrollWidth - clientWidth = 0` |
| 两个把手默认占两端、读数是"不限" | 0 ~ 8 |
| 刻度在滑条下面、一共 9 个、默认全部高亮 | `<1M 1-5 … 100+` |
| 只动右手 = 只带 maxSize；两个都动 = 都带 | 请求参数与挡位表一致 |
| 把手不许交叉（两个方向都验） | 2 <= 4 / 自动交换 |
| 顶栏没有单独的调色板按钮了 | 并进筛选按钮 |

另外修了两处测试自身的坑（都是"元素被滚出视口导致鼠标事件打空"）：
排序端到端在滚动前先把卡片收起来（卡片会占掉图片区高度），
主题色端到端在拖色轮前先 `scrollIntoView` 并断言色轮完整落在视口里。

全量回归：前端 92 + 后端 44 + 生命周期 19 + 九套端到端（33/50/27/14/23/8/16/87/17）
= **430 项全过**。

---

## 2.3.2

### 修复（重要）：扫描素材库扫不出任何文件

新加的「相机」图库（`/vol1/1000/Camera`，全是照片）扫描完是空的，界面只说「扫描完成」。
查下来是目录遍历用的这一行：

```js
glob(path.join(libraryPath, '**', '*.*'), { nodir: true, nocase: true, ... })
```

**带 `nocase: true` 的 glob 在 btrfs 上一个文件都匹配不到**（飞牛的数据盘就是 btrfs），
连「路径完全正确、文件确实存在」也返回空数组。实测：

```
glob('/vol1/.../frontend/src/**/*.js', { nodir: true })              → 47 个
glob('/vol1/.../frontend/src/**/*.js', { nodir: true, nocase: true }) →  0 个
glob('/vol1/.../package.json',           { nocase: true })             →  0 个（文件明明存在）
```

同样的调用放在 `/tmp`（ext4/tmpfs）上完全正常 —— 所以本地测试、端到端测试一直是绿的，
只有真机上「扫描完成，0 个文件」。这个写法是从 FlyPic 继承来的（`nocase` 本来是为了
Windows/macOS 的大小写不敏感），也就是说**自从接手以来，扫描就没在飞牛上真正跑通过**；
老库里那 6000 多张图是更早版本用另一条路径（readdir）入库的，所以一直看着没事。

复现与验证过程（都是在真机上跑的）：

1. 用应用自己的 API 扫「相机」：`/api/scan/rescan` 返回 `{total: 0}`，耗时 0.1 秒
2. 通过上传接口往那个库里写了一个文件，再扫：**还是 0** —— 说明不是"目录里没文件"，是遍历本身找不到
3. 把同一段 glob 调用分别跑在 btrfs（/vol1、/vol2）和 /tmp 上，定位到 `nocase`
4. `glob@10.3.10` 和 `10.5.0` 表现一致 → 不是版本回归，是这个选项在 btrfs 上根本不能用

现在改成自己 `readdir` 递归（`utils/scanner.js` 的 `getAllImageFiles`）：

- 不依赖 glob 的大小写匹配，行为确定
- 跳过 `.niupic` / `.flypic` / `node_modules`（否则缩略图会被当成照片扫进来）
- 子目录读不了就跳过并记日志；**根目录读不了直接抛错**，错误信息里写清楚去
  「应用中心 → NiuPic → 应用设置 → 可访问文件夹」授权
- 顺手支持符号链接（断链忽略、不会循环）

顺带修的两件事：

- **扫描结果里带上「找到 N 个文件」。** 以前不管扫到几个都只说「扫描完成」，
  0 个和 1 万个在界面上长得一样。现在 `scanComplete` 会带上统计，界面明确显示；
  **找到 0 个会直接标红提示**「检查素材库目录是否还在、应用是否有读取权限」。
- **相机 RAW 归到「图片」而不是「其他」。** `FILE_CATEGORIES.image` 补上了
  RW2 / ARW / CR2 / CR3 / NEF / RAF / ORF / DNG 等各家 RAW（sharp 读不了它们，
  会退化成占位尺寸，但它们是照片）。

### 调整

**排序和筛选拆成两张卡片。** 上一版塞在同一张卡里，排序夹在中间像是筛选的一部分。
现在排序单独一张卡置顶，筛选一张卡在下，两张都画在图片区里。

**卡片铺满图片区宽度。** 上一版固定 768px，右边空着一大片（用户原话：
「宽度看着合适，但是会导致右侧有大片空白」）。现在铺满图片区（实测 998/1024px），
内容用栅格分栏，主题色作为右侧一栏并入筛选卡片。

**格式一栏的组头可以点。** 「图片 / 视频 / 其他」这三个组头本身就是开关：
点一下选中整组（照片多的时候比一个个点格式快得多），再点一下整组取消，
部分选中时显示为中间态。

**文件大小：滑块 + 10 个停靠点。** 按要求把固定挡位改回滑块，
但滑块是「一格一挡」的：第 0 格是「不限」，后面依次是
`< 1MB / 1-5MB / 5-10MB / 10-20MB / 20-40MB / 40-60MB / 60-80MB / 80-100MB / 100MB 以上`。
滑条宽度收敛在 28rem（实测 300px），不再横跨整张卡片；
右侧实时显示当前挡位和这一档有多少张。

**顶栏版式。** 搜索框在顶栏正中（实测中心 800 = 屏幕中心 800），
筛选按钮挂在搜索框左侧（`absolute right-full`，不占位、不会把搜索框推偏 ——
用 flex 并排时搜索框会右偏 107px），缩略图滑块、布局、扫描、主题色、明暗切换都在右侧。

### 验证

新增 `browsertest/acc/e2e-scan-camera.cjs`（17 项）：用一棵"像相机卡"的目录树
（大写扩展名 `.JPG/.RW2/.CR2`、中文目录、带空格的目录、深 5 层、无扩展名文件、
`.niupic` 干扰目录、空目录）钉住扫描行为，并验证增量扫描能捡起新增文件。

`e2e-filter-mode.cjs` 扩到 **76 项**，新增：排序卡在筛选卡上面、卡片铺满图片区、
主题色区块在卡片里、组头一键选整组、滑块停在某档时请求带的 minSize/maxSize 与该档一致、
顶栏版式（筛选在搜索框左侧、搜索框居中）等。

新增前端单测 `hooks-import.test.js`：用了 hook 就必须从 react import ——
这一轮给 ScanMenu 加 `useMemo` 时 import 没改对，**构建成功、单测全绿，端到端一跑整页白屏**
（`ReferenceError: useMemo is not defined`），构建工具不做未定义全局变量的检查，所以得自己扫。

全量回归：前端 91 + 后端 44 + 生命周期 19 + 九套端到端（33/49/27/14/23/8/16/76/17）
= **417 项全过**。

---

## 2.3.1

装上 2.3.0 之后提的四条，都是版面和选项粒度上的问题。

### 调整

**排序置顶，和筛选分成两块。**

2.3.0 把排序和筛选做进了同一张卡片，但顺序是「模式开关 → 排序 → 格式 → 大小 → 方向 → 评分」，
排序夹在中间，看起来像是筛选的一部分。现在卡片分成上下两块：

```
排序   创建时间 · 添加时间 · 修改时间 · 文件名 · … · 随机        升序 / 降序
────────────────────────────────────────────────────────────
筛选   [只显示 | 排除]                                      清空（2 项）
       格式  图片  JPEG 110  PNG 46
             视频  MP4 2
             其他  PSD 1  PDF 1
       大小  < 1 MB 117   1 - 5 MB 14   5 - 10 MB   …
       方向  横图 152  竖图 18
       评分  ★★★★★ 3   未评分 173
```

**文件大小：双滑块换成 9 个固定挡位。**

滑块看着"高级"，用起来并不好用：素材库里的文件大小跨度能有 200 倍
（100KB ~ 20MB），要么用对数刻度（用户看不懂「为什么中间是 2MB」），
要么用线性刻度（小文件全挤在最左边几像素里拖不动）。
而且一条滑块横跨整个屏幕，和"选个大概范围"这个需求完全不匹配。

现在是固定挡位，每个挡位后面显示**有多少张**，空挡位置灰不可点：

```
< 1 MB   1 - 5 MB   5 - 10 MB   10 - 20 MB   20 - 40 MB
40 - 60 MB   60 - 80 MB   80 - 100 MB   100 MB 以上
```

挡位表在后端 `constants.SIZE_BRACKETS`（唯一定义处），前端只负责画 ——
两边各写一份"1MB 到底算哪一档"迟早会对不上。统计口径是 `[minSize, maxSize)`，
也就是每张图只算进一个挡位、各挡位计数之和 = 总数。

**格式分成 图片 / 视频 / 其他 三组。**

库里 `jpg / png / mp4 / psd / pdf` 混在一起按条数排时，mp4 和 psd 会插在
jpg 和 png 中间，找图片格式得来回扫。现在按类别分组、组内按条数排：

- 图片（jpg / jpeg / png / webp / gif / …）
- 视频（mp4 / mov / mkv / …）
- 其他（psd / ai / pdf / docx / …）

类别取数据库里的 `file_type`（扫描时就写好的），不是前端按扩展名现猜。

**筛选 / 主题色卡片改成从图片区展开。**

卡片原来画在顶栏里（`<header>` 内部），一展开整个内容行都被压矮 ——
**左右两个侧栏也跟着变短**，而且卡片铺满整屏宽。
现在卡片画在中间那一列里：

```
┌──────────────── 顶栏（高度不变） ────────────────┐
├──────┬──────────────────────────────┬──────────┤
│ 左边栏 │  ┌── 筛选卡片（最宽 768px）──┐  │  右边栏  │
│ 高度  │  └──────────────────────────┘  │  高度    │
│ 不变  │        图片瀑布流              │  不变    │
└──────┴──────────────────────────────┴──────────┘
```

只挤图片区，两侧边栏的高度和顶边都不动；卡片宽度收敛到 768px（`max-w-3xl`），
不再铺满整屏。

### 验证

`e2e-filter-mode.cjs` 扩到 **62 项**，这一轮新增的断言包括：

| 断言 | 说明 |
|---|---|
| 格式顺序 = 图片 → 视频 → 其他 | 接口和界面各验一遍 |
| 9 个挡位的条数和磁盘上真实文件大小一致 | 夹具里放了 2MB / 30MB 的 mp4 和 3MB 的 psd |
| 各挡位计数之和 = 总数 | 口径互不重叠 |
| 点挡位后请求带的 minSize/maxSize 与挡位一致 | 界面总数 = 该挡位统计数 |
| 排序在筛选上面 | `compareDocumentPosition` 比 DOM 顺序 |
| 卡片没压到左右侧栏、宽度 < 图片区宽度 | 比 `getBoundingClientRect` |
| 卡片开合前后两侧边栏高度不变 | 943 → 943 |
| 手机端 9 个挡位自动换行、不横向溢出 | 390px 屏上排 3 行，overflowX=0 |

全量回归：前端 85 + 后端 39 + 生命周期 19 + 八套端到端（33/49/27/14/23/8/16/62）
= **375 项全过**。

---

## 2.3.0

### 新增

**筛选条件可以反过来用：新增「排除」模式。**

面板左上角多了一个开关：

| 模式 | 含义 |
|---|---|
| 只显示（默认） | 只留下符合条件的图片 |
| 排除 | 把符合条件的图片藏起来，看剩下的 |

多选之间是「或」（选 jpg 和 png = 两种都要），不同维度之间是「与」
（选 jpg + 竖图 = jpg 里的竖图）。排除模式是整个条件组的取反。

实现上有一处必须小心：排除一律用 `COALESCE(列, '') NOT IN (...)`。
直接写 `format NOT IN ('jpg')` 时，`format` 为 NULL 的记录会得到 NULL
（既不是真也不是假），整行被悄悄丢掉 —— 那不是用户要的「排除」，
用户要的是「除了 jpg，别的都留下」。

**格式选项只列出当前文件夹里真实存在的，并带条数。**

以前是在前端拿「已加载的第一页（最多 100 张）」统计出来的，
所以点进一个只有 png 的子文件夹，格式列表里还挂着上一层的 jpg / webp；
图片多的文件夹里，第一页没出现的格式也会被当成「不存在」。

现在选项来自后端 `GET /api/image/filter-options?folder=…`，
只统计当前范围（文件夹 / 搜索结果），每个选项后面显示有多少张：

```
格式   JPEG 110   PNG 46   WEBP 20
方向   横图 152   竖图 18
评分   ★★★★★ 3   未评分 173
```

**文件大小改成双滑块区间（类似 PornHub 的时长筛选）。**

原来是几档写死的区间（`< 1MB`、`1 - 5MB` …），既不好用也不准。
现在是两个滑块拖区间上下限，中间高亮，底下带一排大小分布柱状图
（28 个桶，和服务端同一套对数刻度）。

刻度是**对数**的：文件大小普遍是 100KB ~ 20MB（200 倍），
线性刻度会把所有图挤在滑块最左边几像素里，根本拖不动。
对数刻度下每一格代表「乘一个固定倍数」，两端正好是最小和最大的那张。

拖动过程中只更新本地显示，**松手才真正筛选** —— 否则每挪一格就往后端发一次请求。

### 改进

**筛选和排序合成一个按钮、一个面板。**

原来是两个按钮两个面板，而且互相会把对方关掉（`setShowFilters(false)` / `setShowSort(false)`）。
筛选和排序回答的其实是同一个问题 ——「我现在看到的是哪些图、按什么顺序」，
分成两个之后用户得先判断「我这个问题归哪个按钮管」。
现在顶栏只有一个按钮：左边是筛选状态（激活了几项会显示角标），右边是当前排序字段。

**筛选真正下沉到后端。**

以前筛选是前端 `filterImages(images, filters)` —— 只对**已经加载的那一页**过滤，于是：

- 翻页之后的图片完全不筛（点「只看 jpg」，往下滚，jpg 之外的图照样出来）
- 界面上的「已加载 / 总数」里的总数是筛之前的，对不上

这和上一版修掉的「排序只对第一页生效」是同一类问题：**状态只落在前端，
后端不知道你在筛什么**。现在筛选条件（模式 / 格式 / 大小区间 / 方向 / 评分）
全部进 SQL，和排序走同一条 `buildImageQueryParams` 拼装路径。

### 修复

- **大小滑块在「全是小图」的库上拖不动**：位置换算里有个「四舍五入到 KB」，
  180 ~ 879 字节的范围内每个位置都被抹成 0，拖了等于没拖。
  现在 1KB 以下按字节取整，并把结果夹回范围两端。
- **拖完滑块松手后弹回整段**：滑块给的是 `{ min, max }`，
  而 store 的 action 收的是 `{ minSize, maxSize }`，字段名对不上 → 每次都提交成「不限」。
  两个都改成了会断言字段形状的写法（前端单测 + 端到端各钉一遍）。
- 松手事件从滑块本身挪到了外层容器：滑块是 `pointer-events: none`
  （只有滑块的圆形把手接收指针事件），`mouseup` 的目标常常是外层，
  挂在里面根本不会触发。

### 验证

新增 `browsertest/acc/e2e-filter-mode.cjs`（45 项），用 176 张图（jpg 110 张，**超过一页**）
专门验证「筛选在后端生效」：按 jpg 筛完应当是 110 张（要翻页）而不是 100 张。

全量回归：前端 89 + 后端 37 + 生命周期 19 + 八套端到端（33/46/27/14/23/8/16/45）
= **357 项全过**。

---

## 2.2.2

### 修复

**顶栏的大小滑块其实没有在控制图片大小。**

排版原本是「把整行拉伸到填满宽度」（justified），于是：

```
行高 = 容器宽度 ÷ 该行所有图片的宽高比之和
```

这个式子跟滑块**没有任何关系**。滑块只影响「一行能放几张图」，所以：

- **分组没变的行，拖滑块时尺寸纹丝不动** —— 视频、PSD 这类尺寸完全相同的文件
  （后端给的占位尺寸都是 640×480）最容易凑成这种「稳定分组」，看起来就是「怎么拖都不变」
- 实际高度可能远超标尺值：**滑块拉到 300px 时，图片实测 365px**

现在改成**每张图的高度就是滑块值**：宽度 = 高度 × 宽高比，从左往右排，
放不下就换行（超宽全景图才整体缩小以适应容器）。
代价是每行右侧可能留白 —— 这正是上一版「单图不撑满」想要的效果，现在全站一致。

实测：滑块 150/200/250/300 → 图片高度就是 150/200/250/300，
视频、PSD、方图、竖图全部跟着变。

### 新增

- **拖动滑块时画面不再上下乱窜（滚动锚定）**：改尺寸会让所有行重新排布，
  但滚动位置还是旧的像素值，于是画面里的图会整体跳走。
  现在会记下「视口顶部那张图」，重排之后把它放回原来的位置。
  锚定的是**图片**而不是「第几行」—— 改尺寸后每行能放几张会变，
  同一张图会换到别的行去，只记行号它照样会跑。
  实测：滑块 200 → 260（图片高度从 200 变到 260）时，
  原来在 `top=75` 的那张图**仍然在 `top=75`**。

---

## 2.2.1

### 修复

| 问题 | 原因 |
|---|---|
| 开启「按文件夹分隔」后，**只有一张图的文件夹会被放大到整屏那么高** | 「不拉伸」的保护只写在「最后一行」的分支里（`isLastImage && currentRow.length < 3`）。分隔功能会在文件夹边界强制换行，于是「一行只有一张图」变得很常见，而它不在保护范围内，就按 `容器宽度 ÷ 图片宽度` 放大，一张图能拉到整屏。现在**任何单图行都不再撑满**：按顶栏缩略图滑块设定的高度原尺寸显示，右边留空（超宽全景图仍会缩到容器内，不会溢出） |
| 展开/收起顶栏面板（排序 / 主题色 / 布局）之后，**图片区底部留下一块空白**，刷新页面才恢复 | 容器尺寸监听里只比较了**宽度**：`if (Math.abs(width - lastWidth) < 12) return`。面板开合改变的是**高度**（内容区从 906px 变到 778px），宽度没变就直接返回，`containerHeight` 一直是旧值，虚拟列表按旧高度渲染，底部就空出 128px。现在宽度和高度分开判断：宽度仍有 12px 阈值（重算布局贵），高度 0 阈值（只是可视区大小，必须立刻跟上） |

> 第二个问题是**祖传 bug**，不是排序面板引入的 —— 排序面板只是恰好会改变头部高度。
> 任何让内容区高度变化而没有改宽度的操作都会触发它。

---

## 2.2.0

### 修复

**排序只对第一页生效。**
翻页加载（`useInfiniteScroll`）自己手写了一份请求参数，**漏了 `sort` / `order` / `seed`**，
于是第 1 页按你选的排序，第 2 页起全部退回后端的默认排序（创建时间倒序）。
现在首屏和翻页共用同一个参数构造函数，并加了单测钉死「只允许有一处拼参数」。

**瀑布流的空白条远大于行间距。**
两个原因叠加：

1. 行高写死 `+32`，再加上图片下方文件名那一行的 28px，两张图之间实际是 **62px**；
2. 更要命的是 react-window 的**行高缓存从不刷新** ——
   `ImageWaterfall.jsx` 里本该是 `useEffect` 的地方写成了 `useRef`，
   而 `useRef` 的回调**永远不会执行**，`resetAfterIndex` 一次都没跑过。
   结果只要重新排版过（拖侧栏宽度、拉缩略图大小、改行间距），
   每行实际高度变了但列表还按旧高度摆放，多出来的差就变成一条条大段白带。

现在行间距就是设置里的值（默认 32px，实测 33px），
并且任何影响所有行的排版变化都会重新计算行位置。

**「扫描库文件」不处理改动过的文件。**
增量同步里 `toCheck` 只被用来数了个数（`modifiedCount`），**从来没被真正重新处理**，
所以在磁盘上替换/编辑过的图片，刷新多少次都不会更新。

**重扫会让评分和收藏归零。**
入库用的是 `INSERT OR REPLACE INTO images`，而 `path` 是 UNIQUE ——
SQLite 的 REPLACE 语义是「删掉旧行再插新行」，没列进 INSERT 的
`rating` / `favorite` / `tags` 全部回到默认值。
已改成 `ON CONFLICT(path) DO UPDATE`（同时不再刷新 `indexed_at`，
否则按「添加时间」排序会乱）。
> ⚠️ 这个雷在「全量重扫」上线前必须先拆掉，否则第一次重扫就会清空所有评分。

### 新增

- **全量重扫（扫描库文件）**：对齐 Emby / Jellyfin 的「扫描媒体库」。
  每个文件都重新读取元数据、重算名称排序、补齐缺失的缩略图，
  并清理磁盘上已不存在的记录（清理前有护栏：一次要删掉超过一半就跳过，
  多半是路径或权限出了问题）。**评分、收藏、标签保留。**
  顶栏原来的「刷新」按钮变成一个菜单：增量扫描 / 全量重扫。
- **面板宽度只能从顶部把手拖动**：原来是一条通高的 4px 拖拽条、
  两侧还各扩了 20px 的透明点击区，整条分界线从上到下都能触发拖拽 ——
  想滚页面、想拖滑块，鼠标稍微偏一点就变成改侧栏宽度。
  现在分界线只负责「看」（`pointer-events: none`），
  只有按住顶部那个把手才能调宽度，双击把手恢复默认。左右两侧都改了。
- **按文件夹分隔**：开启后，不同文件夹的图片之间画一条横线隔开（并自动换行）。
  文件夹很多的时候找图清楚不少。
- **行间距可调**：0～64px（含文件名那一行时最小 28px），默认 32px。

---

## 2.1.2

### 修复

**更新版本不再等于重装 —— 这是本次最重要的修复。**

通过应用中心「手动安装」来更新版本时，飞牛会先把旧版本按**卸载流程**走一遍再装新版本。
而 NiuPic 的卸载脚本里有一句无条件删除配置文件：

```bash
rm -f "$TRIM_PKGVAR/config.json"
```

于是每次更新都会丢掉：**素材库列表、访问密码、JWT 密钥、主题色、界面偏好** ——
等于重装一遍，得重新添加素材库、重新设置。系统日志里看得清清楚楚：

```
14:18:24 - Stopping NiuPic...
🧹 NiuPic 卸载清理...
清理配置文件: /vol3/@appdata/niupic/config.json
14:18:38 - Starting NiuPic...
```

修法有两层：

1. **卸载默认什么都不删**。只有用户在卸载向导里明确选了「全部删除」才会清理，
   并且加了路径护栏（只认 `.../@appdata/niupic` 这种形态，变量为空或异常时不删）。
   官方文档也明确要求「卸载逻辑不得擅自删除用户数据」。
2. **配置自动备份 + 自动恢复**。每次保存配置都会同时写一份 `config.json.bak`；
   安装时如果发现 `config.json` 不在而备份还在，就自动恢复。

同时新增了 `wizard/uninstall` 卸载向导，把「保留 / 全部删除」交给用户选，默认保留。

> ⚠️ **从 2.1.1 升级到本版时仍会丢一次配置** —— 因为执行删除的是**已经装在机器上的
> 2.1.1 的卸载脚本**，新版脚本要等这次装完才生效。
> 想连这一次都不丢：安装前先把配置复制一份备份即可，新版安装时会自动恢复：
>
> ```bash
> cp /vol3/@appdata/niupic/config.json /vol3/@appdata/niupic/config.json.bak
> ```
>
> 从 2.1.2 开始，以后每次更新都会自动保留配置。

---

## 2.1.1

### 修复

| 问题 | 原因 |
|---|---|
| **手机上点「排序」整页白屏** | 排序面板的渲染函数写成了 `const renderSortPanel = ...`，而移动端分支在它上面就引用了它 —— 一渲染就报 `Cannot access 'renderSortPanel' before initialization`，React 整棵树卸载。桌面端恰好是在声明之后才用到，所以从 2.0.8 加上排序功能起一直带着这个 bug，手机端排序面板从来没打开过。改成 `function` 声明（会提升）后正常 |

### 调整

- **排序里去掉「格式」**：和「文件类型」重复，排序字段从 15 个减到 14 个
- 旧版本在浏览器里存过「格式」排序的，会自动回到默认排序（创建时间 · 倒序）
  —— 连方向一起重置，不会留下「创建时间 + 升序」这种看着别扭的组合
- 后端同步删掉了 `format` 排序白名单项；老客户端如果还传 `sort=format`，
  会退回默认字段排序，不会报错

---

## 2.1.0

### 新增

- **主题色可自定义**（原来固定是蓝色）
  - **12 种预设颜色**：默认蓝、天蓝、青色、青绿、翠绿、琥珀、橙色、红色、玫红、紫罗兰、靛蓝、石板灰
  - **色轮**：角度定色相、半径定饱和度，另有明度滑块和十六进制输入框，想要什么颜色都能调出来
  - 边调边生效，停手 400ms 后自动保存；换主题色后**刷新、重启都还在**
  - 面板里直接给出「主按钮 / 次要按钮 / 标签 / 11 档色阶」的实时预览，选之前就能看到效果
  - 「恢复默认」一键回到蓝色
  - 桌面端在顶栏点调色板图标，手机端在「设置」面板里（同一个面板）

### 实现方式

界面里 150 多处用的是 Tailwind 的 `bg-blue-500` / `text-blue-600` 这类类名。
这次把 `blue` 这一族调色板整体**重映射到 CSS 变量**（`--accent-50` ~ `--accent-950`），
换主题色只需改 11 个变量，不用动一处 JSX，也不会漏掉某个角落。

### 可读性保护

不是把颜色原样套上去就完事：太亮的颜色（比如纯黄）会让「白字按钮」看不清，
太暗的颜色在暗色模式下没有层次。所以选中的颜色会经过一次收敛：

- 保留色相与饱和度，明度尽量贴近可用区间
- 保证 **白字压在强调色上 ≥ 3:1**、**强调色当文字放在暗色背景上 ≥ 3:1**
- 再按基础色深浅生成 50~950 的色阶（亮端始终是浅色，暗端跟着主题色走，明度严格递减）

面板里的「实际效果」色块显示的就是收敛后的颜色，所见即所得。

### 修复

| 问题 | 原因 |
|---|---|
| `server.js` 里 `configManager` 是手写白名单，新增配置项忘了同步会让接口 500 | 改成从 `utils/config` 的导出自动展开（主题色第一次就是这么挂的：界面看着变了，其实根本没存下来） |

---

## 2.0.9

### 新增

- **添加素材库时直接选「可访问文件夹」，不用再手输路径**
  - 飞牛的文件夹权限是管理员在**应用中心手动授权**的，能访问的目录本来就只有那几个；
    NiuPic 现在把这份清单直接列出来给用户选
  - 清单里会标出每个目录的状态：`只读`、`已添加`、`路径不存在`、`已有索引`、
    `应用共享目录`、`自动探测`；不可用的条目直接置灰，点不了
  - 选中文件夹后**素材库名称自动跟随**（用户自己改过名称就不再覆盖）
  - 素材库目录里已经有 `.niupic` 索引时会显示 `已有索引`，添加后直接复用，不用重扫
  - 列表右上角有刷新按钮：管理员在应用市场改完授权，点一下即可看到最新的
- 安装向导改为「第一步授权文件夹 → 第二步添加素材库」，
  不再教用户去翻文件系统路径
- 现在会**校验素材库路径是否在授权范围内**（旧版本对任意路径都放行）：
  未授权路径会被拒绝，并提示去「应用设置 → 可访问文件夹」里添加

### 修复

| 问题 | 原因 |
|---|---|
| 权限不足的提示文案过时 | 提示用户去「数据共享」里加权限，而飞牛实际的入口是「应用设置 → 可访问文件夹」 |

### 权限模型（本次新增，安装/升级必看）

为了让应用能读到「哪些目录被授权了」，包内新增了三处声明：

| 文件 | 新增内容 | 作用 |
|---|---|---|
| `manifest` | `disable_authorization_path = false` | 打开应用市场的「可访问文件夹」管理入口 |
| `config/resource` | `api-scope: ["trim.file.sharedAccess", "trim.file.path"]` | 允许后端查询授权目录清单、把内部路径转成可读路径 |
| `config/privilege` | `join-groups: ["TrimApiUsers"]` | 应用用户需要属于该组才能连上飞牛开放 API 的 Unix Socket |

**升级后如果「可访问文件夹」里还是空的，请重装一次应用** ——
飞牛只在安装时写入 api-scope 与用户组。

---

## 2.0.8

首个以「牛图 NiuPic」名义发布的版本，在 FlyPic 1.3.x 基础上继续开发。

### 更名与品牌

- 项目更名为 **牛图 NiuPic**，换用新 logo
- 应用 ID 由 `flypic` 改为 `niupic`（安装目录、数据目录、共享目录名同步变更）
- 素材库内的索引目录由 `.flypic` 改为 `.niupic`
- 界面文案、浏览器标题、导出文件名、日志、安装向导全部更新为牛图 NiuPic
- **保留原作者署名**：原作者 [ZangXincz](https://github.com/ZangXincz)，
  在应用简介、更新日志、`package.json`、`LICENSE` 与 README 中均有标注

### 新增

- **图片排序**：首页、全部图片、任意子文件夹都能排序，排序偏好跨层级、跨刷新保留
  - 15 个排序字段：创建时间、添加时间、修改时间、文件名、文件大小、分辨率、宽度、高度、
    宽高比、格式、文件类型、评分、收藏、所在文件夹、随机
  - 文件名用**自然排序**（`img2` 排在 `img10` 前面），不是字典序
  - 排序在**后端**完成：前端一次只取一页，本地排序会把翻页顺序弄乱
  - 随机排序用固定种子，所以翻页不会重复或漏图；再点一次「随机」重新洗牌
- **双击图片进入沉浸式全屏查看器**，交互对齐 Windows「照片」：
  - 左右切换：`←` `→`、空格、Backspace、Home / End、鼠标拖拽、触屏滑动、两侧箭头按钮
  - 缩放：滚轮以光标为锚点缩放、双击在「适应窗口 / 100%」间切换、`+` `-`、拖拽平移、工具栏缩放条
  - 旋转：`[` 左旋、`]` 右旋，旋转后自动重算适应比例
  - 幻灯片播放；`I` 打开文件信息面板；`F` 切换全屏；`Esc` 退出
  - 工具栏与标题 2.6 秒无操作自动隐藏（沉浸式）
  - 打开期间吞掉全局键盘事件，避免网格的删除/评分等快捷键误触发
- 首页直接展示素材库**全部层级**的图片，不需要先点进子文件夹
- 从 FlyPic 迁移的辅助脚本 `scripts/tools/migrate-flypic-dir.js`
  （把 `.flypic` 就地改成 `.niupic`，免去重新扫描和重建缩略图）

### 修复

| 问题 | 原因 |
|---|---|
| 打包后应用无法启动 | `bcrypt` 原生模块被放到多一层的 libc 子目录，`node-pre-gyp` 找不到 |
| 「添加图库」报 `Internal server error` | `LibraryService` 用了 `logger.*` 却没有 `require` |
| 图片超过 50 张后双击无反应 | 超过 50 张走 react-window 虚拟列表，首次点击引发的重渲染会重建行 DOM，浏览器因此不派发 `dblclick`。改为在父组件按「同一张图 + 400ms 内两次点击」自行判定 |
| 分辨率显示成缩略图尺寸，缩放比例全错 | 扫描时把缩略图的宽高当成原图尺寸写进了数据库。现在记录原图真实像素，并在扫描时自动纠正历史数据 |
| 「全部图片」看不到任何照片 | `MainContent` 把「未选文件夹」当成了「什么都不显示」，直接跳过了图片加载 |
| 手机竖拍（EXIF 旋转）的缩略图被裁成横图 | 目标尺寸用了旋转前的宽高，`.rotate()` 之后 `fit:cover` 把竖图裁掉了 |
| 顶栏排序面板的说明文字显示不全 | 桌面端排序面板被放进了固定高度（`h-14`）的工具栏容器里，且顶栏作为 flex item 默认可被压缩。改为把面板放到工具栏之外，并给顶栏加 `flex-shrink-0` |

### 兼容性说明

- 素材库的**索引格式与 FlyPic 完全一致**，用迁移脚本改名后即可直接使用，无需重新扫描
- 素材库里遗留的旧 `.flypic` 目录会被自动忽略，不会被当作图片索引进来
- 因为应用 ID 变了，安装牛图后需要重新添加素材库、重设访问密码

---

## 上游版本（FlyPic）

2.0.8 之前的历史见原项目：[ZangXincz/FlyPic](https://github.com/ZangXincz/FlyPic)
